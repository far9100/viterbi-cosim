# CHANGELOG

## 2026-07-31（M10：`make repro` 已經壞掉了，而且是六處各自獨立的腐化）

- `2026-07-31-01` debug — **`m7-repro` 坐實的那個「一鍵冷跑」宣稱，從 M9 落地那天起就是假的，而且有三處各自獨立的成因。** (1) `scripts/repro.sh` 的步驟串沒有 `make m9`，但第 2 步 `rm -rf data`，於是 `make figures` 的 `plot_m9.py` 開不到 `data/power_m9.json` ⇒ 冷跑在 figures 步驟 FileNotFoundError；(2) 就算跳過圖，`check_paper_numbers.py` 斷言 `len(GATES) == 36`，少了 M9 的 8 列只會有 28 列 ⇒ `make report` 同樣紅燈；(3) **與前兩者完全無關的第三處**：`ppa/run_power.py` 的 `point()` 在 M9 期間新增了 `seed` 欄，而 2026-07-17 冷跑產出並提交的 `data/power.json` 沒有這個鍵，`_no_timing()` 只剝 `sim_s`/`wall_s` ⇒ 熱跑因 `data/cache_m5/` 命中而完全看不見，**冷跑則必然多出 `"seed": null`**、逐位元組判準必敗。三處都修在根因：M9 進 repro / all / gates 鏈路；`evidence_only()` 在 `seed is None` 時整個鍵拿掉（`seed` 有值時照樣入檔，那是 null 分布重現所必需）；成功橫幅的 gate 數改由 `data/gates.csv` 現算——它原本硬寫「26 個 gate」，是冷跑路徑上唯一沒有檢查器盯著的數字，所以它腐化了兩次都沒被發現。
- `2026-07-31-02` debug — **`data/power_m9.json`（git 追蹤的證據檔）帶著 `sim_s` / `wall_s` 進了庫**，與 `2026-07-16-06` 修掉的是同一個病：`scripts/m9_sweep.py` 沒有沿用 `run_power.py` 的剝除函式，自己 `json.dump(rows)`。計時遙測每次都會變，留著就永遠無法逐位元組重生。剝除後 diff 為 **0 新增 / 32 刪除**（16 個點 × 2 個欄位），功耗數值一位未動。同時給 `m9_sweep.py` 與 `m9_null.py` 補上與 `run_power.py` 相同的時間預算 → `return 1` 續跑語意（48 個閘級點跑數小時，harness 有 10 分鐘上限），並確保**半份 null 分布不寫檔**——少幾個 seed 的 σ_null 照樣算得出數字，而那個數字會被當成雜訊地板用掉。
- `2026-07-31-03` debug — **第四處腐化，而且它讓 `make gates` 在熱樹上永遠過不了：G6 負向 gate 判成 0/4。** `tb/cocotb/run_tier_a.py` 的 pass-marker 只存 `(frames, stages)` 兩個數字，命中就 `continue` ⇒ 那一組的「G6 觸發（stage N, spread A > B）」**證據行根本不會被印出來**；而 `scripts/m3_gate.py` 的 G6 負向 gate 正是 grep 這些證據行裁決的（`len(fires) == 4`）。只要 g6neg 的 4 個 marker 存在，這道零容忍 gate 就必然失敗，`make m3` / `make gates` 只有冷跑（`repro.sh` 會刪掉 `tb/cocotb/build`）才綠。**這個不對稱本身有價值**——`2026-07-16` 的第一次冷跑正是靠「g6neg 要求新鮮的違規證據，而 c2 信任聚合計數器」才抓到 stale marker 讓 M3 假性通過——所以修法不是讓 marker 去存證據（那會毀掉這個不對稱），而是**不快取 g6neg**：它只有 4 組、實測重跑 **1.6 秒**。用 1.6 秒換一道 gate 的全部證據。
- `2026-07-31-04` debug — **`scripts/gates.py` 的 milestone 排序表寫死到 M9**（`{f"M{i}": i for i in range(10)}`），上限剛好卡在專案當時的最後一個里程碑。一旦出現 M10，它和之後的每一個里程碑都會落到同一個 fallback 鍵，穩定排序退化成「哪一支 gate 最後跑」⇒ `gates.csv` 的列序重新取決於執行順序，正是這段排序當初要消滅的東西；而且它不會報錯，只會讓冷跑的逐位元組判準偶發性紅燈。改為解析里程碑編號，在現有 36 列上排序結果為**恆等**（檔案不變）。同時 `Makefile` 的 `gates:` 補上 `m0_gate.py` 與 `m9_gate.py`——`finalize()` 是以 milestone 為單位整批取代，沒被列進來的里程碑其列只會沿用舊值、永遠不會被重新驗證，而這個「跑全部 gate」的入口當時只涵蓋 36 列中的 25 列。
- `2026-07-31-05` refactor — **SAIF 歸檔改為以里程碑為範圍，並把 M9 的 32 個點補進庫。** 第一版無條件掃 `data/saif/*.saif` 寫死單一 manifest，當時目錄裡只有 M5 的 10 個檔所以看不出問題；M9 落地後目錄裡有 42 個檔，**一次增量的 `make m5` 就會把 M9 的 32 個 SAIF 壓進 M5 的 manifest**，把「M5 功耗證據的清單」悄悄變成「碰巧在硬碟上的所有 SAIF 的清單」。完整冷跑剛好遮住這個 bug（目錄從空的開始），這正是它沒被發現的原因，也正是判準不能只靠冷跑的理由。`scripts/verify_saif_gz.sh` 有同一個 bug（把 42 個 `.gz` 全餵給 `saif_toggle.py`，比對必然不同，而失敗訊息會**把範圍設定錯誤偽裝成證據損毀**），一併修。M9 依作者裁決全數歸檔：32 個點 **1114 MB -> 43 MB（26.1x）**，寫進獨立的 `MANIFEST_m9.sha256`；不歸檔的話 M9 的 −42.7%、分區塊佔比與 σ_null 在 repo 內就沒有可重新驗算的原始證據，而那正是 M5 當初決定付這筆空間的理由。M5 的 manifest 重生後逐位元組不變，42 個檔的 SHA-256 全部 OK。
- `2026-07-31-06` refactor — **變異測試不再 `sed -i` git 追蹤中的 `docs/report.md`。** 它是 `make all` 與 `make repro` 的最後一步，跑在數小時冷跑的尾端，卻靠 `trap EXIT` + `/tmp/report.bak` 還原——被 SIGKILL、`/tmp` 滿了或機器重開，都會留下一份被改壞的追蹤文件，而且看起來像是有人手動改壞的。改為複製到 `mktemp -d` 只改副本，檢查器以新增的 `FEC_REPORT_PATH` 指過去；真正的文件從頭到尾沒被寫過，也就不需要還原機制。收尾再加一道 `git status --porcelain -- docs/report.md` 的自我檢查。6 個變異的內容與判準（exit code）一字未改，仍 6/6 全抓到。另外 `check_paper_numbers.py` 的 milestone 存在性清單補上 M9 並加反向檢查（出現未登記的里程碑也要紅燈），避免下一個里程碑再被總數靜靜吸收掉。

- `2026-07-31-07` debug — **第五處腐化，由完整冷跑逼出來，而且它的意義超出建置：`make m9-verify` 驗的從來不是 M9 量的那批 netlist。** `ppa/verify_cg.py` 的 `__main__` 寫死 `cg=True`，組出的 tag 是 `Q{Q}_W{W}_D{D}_cg`——那是由 `rtl/` 合成、M9 開發早期留下的一批 netlist；而 M9 發表的 −42.7% 功耗與 −11.02% 面積是在 `rtl_lowpower/` 合成的 `_rtlv` / `_cg_rtlv` 上量的（`scripts/m9_sweep.py` 的 `VARIANTS`）。**也就是說，已發表的功耗宣稱所依據的 netlist 從來沒有被這道 C2 驗過**——而這道 C2 正是 `docs/lowpower_baseline.md` §4.1 用來擋住「功能完全壞掉但 SAIF 與功耗數字照樣漂亮」的唯一防線，也正是 `2026-07-29-17` 抓到 clock gating 毀掉同步 reset 的那道防線。**在熱樹上這個錯誤是隱形的**：`net_*_cg.v` 剛好還躺在 `ppa/out/` 裡，於是它驗了一批無關的 netlist 然後回報全部通過；冷跑把 `ppa/out/` 刪光之後，沒有任何步驟會重建 `_cg`，四個組態全部「netlist 不存在」而失敗。附帶一個順序 bug：合成藏在 `m9_sweep.ensure_netlists()` 裡，而 Makefile 的相依是 `m9-sweep: m9-verify`，冷跑時 verify 先跑、一個 netlist 都還不存在。修法：`verify_cg.py` 改為迭代 `VARIANTS` × 4 個組態共 8 個 netlist，並自己先確保 netlist 存在，讓順序回到「先合成 → 再 C2 → 才量功耗」。**8 個 netlist 全部通過**（各 1024 個解碼位元，C2 錯 0、X/Z 錯 0），且從零重合成的面積與已發表值吻合（`Q3_W8_D32_rtlv` 163,888.4 vs 發表 163,888 µm²；`_cg_rtlv` 140,165.7 vs 140,166 µm²）⇒ **M9 的宣稱站得住，缺的只是那道 gate 從來沒真的去驗它。** 另把 `m9_sweep.py` / `m9_null.py` 的時間預算改從環境變數讀（比照 `sweep/grid_runner.py`），否則續跑這條路徑只會在真正的冷跑裡第一次被執行到。

- `2026-07-31-08` debug — **第六處：冷跑的判準本身有一個 `data/` 之外的盲點。** `scripts/repro.sh` 第 4 步只跑 `git status --porcelain -- data`，但 `make freeze` 會重寫 `vectors/MANIFEST.json`——**那是凍結的測試向量清單，而且它在 `data/` 之外**。於是每一次冷跑都會默默改動它而沒有任何人看見，`m7-repro`（2026-07-17）坐實的宣稱也繼承了這個盲點。實測差異只在 `metadata` 區塊（`start_timestamp`、`git_commit`，以及當初沒 source `env.sh` 而記成 `absent` 的 verilator / iverilog 版本），46 個向量的 92 個 SHA-256 與 `vectors/*.npz` 全部逐位元組重生。修法不是整份豁免這個檔（那等於豁免掉凍結的本體），而是新增第 4a 步：**比對「除 metadata 以外」的 JSON 必須完全相同**，且 `vectors/` 底下任何非 MANIFEST 的差異一律失敗。精確地豁免該豁免的，不放寬該守的。

- `2026-07-31-09` test — **完整冷跑通過，範圍第一次涵蓋 M9：229 分，除 `meta_*.json` 與 `vectors/MANIFEST.json` 的 metadata 區塊外，每一個檔案逐位元組相同。** 刪光 `data/` + `ppa/out/` + `figures/` + 所有 build 狀態，從零重生 36 筆 gate 記錄、全部 `results_m*.csv`、42 個 `saif/*.saif.gz`（M5 的 10 個 + M9 的 32 個）、兩份 SHA-256 manifest、`tierb_manifests/*.json`，以及 46 個凍結向量的 92 個 SHA-256。各步驟耗時：M0 env 8 秒、M1 golden 694、freeze 1、M2 sweep 5119、M3 rtl 358、M4 tierb 843、M5 ppa 2185、**M9 lowpow 4547**、figures 7、report 0、mutate 1。收尾 `assertions: 226 / mismatches: 0`、變異測試 6/6。**這是第一次有一條連續的冷跑同時涵蓋 M0–M5 與 M9**——`m7-repro`（2026-07-17）涵蓋的是 M0–M5，其宣稱從 M9 落地起就已失效，本次以 `m10-repro2` 重新坐實且範圍更大。順帶修掉本次新加的耗時總表的一個 bug：步驟名含空格（"M0 env"），用 `set -- $e` 以空格切會把秒數讀成名字的第二個字，改用 `|` 當分隔符。

- `2026-07-31-10` add — **凍結文件的勘誤機制：原文一字不改，另外追加。** CLAUDE.md §5.1 規定凍結文件不得回頭修改，但副作用是有五條被推翻的敘述，更正只寫進了 report/thesis ⇒ **單獨閱讀那份凍結文件的人看到的仍是被推翻的說法**。新增 `docs/errata.md` 當索引，四份凍結文件在檔尾追加 `▼▼▼` 帶（沿用 `falsification.md` 在 M5 之後追加裁決時發明的格式）。E-01 energy_model「灌大約 200 倍」→ 漏電只佔總功耗 0.00027%、E_dec 實測只變 0.055%；E-02 λ 與 L₁ 的印刷誤差；E-03 traceback「D=64 接近最佳」撤回（±0.076 dB 雜訊地板內、非單調且有負損失）；E-04 falsification「Q 之間的相對比較不受影響」方向反了（Δd\* 是**下界**）；E-05 lowpower_baseline 的 R² 門檻在 n=5 下判別力不足。`check_paper_numbers.py` 新增 §6 把紀律變成檢查：(a) 每份凍結文件在第一個 `▼▼▼` 之前的內容必須逐位元組等同於其凍結 tag 的 blob（唯一允許的差異是追加 band 時插入的純分隔符）；(b) 索引與各文件的帶雙向對帳。變異測試 6 → 8 種，新增的兩種分別去改凍結本體、加一列對不上的索引，都被抓到。
- `2026-07-31-11` audit — **M9 的「預先登記」沒有時間戳證據，而這是配對重構才讓它第一次被檢查出來的。** §4 的時序檢查原本是全域 max/min，把 `lowpower_baseline.md`（7/29 登記）加進去會晚於 `power.json`（7/14 量測）而誤判紅燈——於是它被排除在檢查之外，**理由只是「加進去會紅燈」**。改成 (登記文件, 量測產物) 配對之後，M9 第一次被驗，結果是：`docs/lowpower_baseline.md` 與 `data/power_m9.json` 的 commit **只差 60 秒**（10:27:32 與 10:28:32），而且 M8/M9 的四個 commit 全部落在 **82 秒**內、與 CHANGELOG 記載的工作日期（07-29、07-30）不一致——那批工作是後來一次補 commit 的。對比 M5 那一對是 **21.2 小時**、跨工作階段。**60 秒分不出「先寫登記文件再量測」與「量完之後補寫文件、兩者一起 commit」**，而後者正是預先登記唯一要排除的情形。處置：新增 `PREREG_MIN_GAP_H = 1.0` 門檻，低於門檻**預設紅燈**；但那幾個 commit 已經發生、重寫歷史只會讓證據更差，而永久紅燈等於廢掉整條流程，所以把「無法補救的事實」轉成「強制且可驗證的揭露」——低於門檻的配對必須在報告與 README 逐字寫出實際間隔，檢查器驗那段揭露存在且數字正確才放行（report §5-7、README 誠實界線第 6 條）。**M9 的事前預測仍然可信，但依據的是文件內容與 CHANGELOG（P1 登記 < 10%、實測 −42.7%，是被自己推翻的——事後補寫不會這樣寫），不是 commit 時間戳；讀者不應把它與 M5 等同視之。**
- `2026-07-31-12` add — **M8/M9/M10 的結果補進所有對外文件。** `docs/report.md` 新增 §7 M9 專章（`figures/fig_m9_states.png` 第一次被文件引用），並套用 `2026-07-30-02` 指出、一直未套用的措辭修正：§4 標題與 §6 結論的「那條曲線不存在」語氣強於資料，改為「效應被侷限在累加器、僅佔約 1% 總功耗，本量測解析不出」（t = 1.82、區間跨過零）；thesis 同步兩處。README 補 M8/M9/M10 三列並如實限縮 M7 的宣稱範圍（M0–M5）。規格書 §9 補 M7–M10 四列、§0 加 M8 文獻稽核的修訂框（d\* 降為 motivation，SNR 無關性改述為「已知；本文貢獻是機制、界與流程後果」）。寫 §7 時覆蓋掃描擋下一件事：**M9 的面積數字（+4.04% / −11.02% / −14.47%）根本不在任何 CSV 裡**，只活在 `rtl_lowpower/README.md` 的散文與 CHANGELOG，違反 §5.4；沒被抓到只是因為當時沒有任何受稽核的文件去引用它們。`m9_gate.py` 因此新增 `data/results_m9_area.csv` 與 null 全距兩個欄位（**不新增 gate**——面積沒有凍結門檻，gate 數維持 36）。assertion 226 → 247、mismatches 0、變異 8/8。

- `2026-08-01-01` test — **G2a 的 oracle 第一次被測試釘住，而且 `make test` 現在真的涵蓋掃描層。** `golden/bounds.py` 算出來的 c_d 是 G2a 的上界來源——整條 BER 鏈路拿它驗證解碼器。它的 docstring 說得很清楚「抄來的表若與多項式對不起來，G2a 就會拿一個錯誤的上界去驗證一個錯誤的解碼器，而且很可能通過」，於是選擇自己枚舉；但那只堵住「抄錯表」，**枚舉本身錯了沒有任何東西會發現**——文獻值只寫在散文註解裡，而註解不會執行。新增 `tests/test_bounds.py`（9 條）：a_10=11 / a_12=38 / a_14=193、c_10=36 / c_12=211 / c_14=1404 逐一釘住；並加三條不依賴文獻表的結構檢查（d_free 與 `Trellis.free_distance()` 的獨立實作交叉比對、奇數重量必須不存在、加大 d_max 不得改動已算出的低階項），以及 Q(0)/Q(1)/Q(2) 的 known-answer 與「硬判決的界必須比軟判決差」（兩支函式的 Es/N0 換算最容易在這裡出錯）。同時 `make test` 從 `pytest tests/` 擴為 `pytest tests/ sweep/test_c2prime.py`：**C2′ 的 47 條零容忍比對先前只有 `make m2` 走得到**，開發時跑 `make test` 看到全綠卻完全沒碰到掃描層的正確性，而 M2 的 BER 全部由那一層算出。測試數 37 → 93。

- `2026-08-01-02` test — **`ppa/` 的解析器從零測試到有測試，而且第一支測試就抓到一個真的 bug。** 這一層是一千多行對 Yosys / OpenSTA / iverilog 輸出的 regex，先前一條測試都沒有；它的失效模式與別處不同——**解析錯了不會拋例外，只會安靜地產生一個看起來很正常的數字**。新增 `tests/test_ppa_parsers.py`（15 條），把各 docstring 記載的五個**已經出貨過**的 bug 逐一寫成回歸案例：跨行 `$timescale`（單行 regex 比不到就靜默退回 "1 ns"，實際是 ps ⇒ SAIF 的 DURATION 差 1000 倍）、段落標題 `=== design hierarchy ===` 含空白（`(\S+)` 比不到 ⇒ 總面積一直是 0）、模組名含單引號（`s32'` 讓 `'[^']+'` 提早收尾）、Yosys 每行是「數量 面積 cell 名」數量在前（寫反 ⇒ DFF 數全是 0）、`report_power` 與 `-instances` 兩種排版共用一支解析器。fixture 取自真實 stat 檔的片段，不是憑空捏的格式——第一版憑空捏的格式被測試擋下來，這件事本身就說明格式不能用猜的。
- `2026-08-01-03` debug — **`ppa/synth.py` 的 `_dff` 比不到有 enable 的 FF，`rtl_lowpower` 的 flop 數少算 97.6%。** regex 寫 `sky130_fd_sc_hd__df\w+`，比不到 `__edfxtp_1`。**在 B0 上看不出來**：Yosys 把 `rtl/` 全部映成 dfxtp、edfxtp 是 0 個，所以 M5 已發表的 2429 個 flop 本來就是對的（修正後仍是 2429，逐位元組不變）。但 `rtl_lowpower/` 的 `if (rst || en)` 改寫讓 Yosys 推導出 enable-FF，於是 B0′ 的 2429 個 flop **只有 59 個被算到**。修正後三個狀態都回報 2429 —— 同一個設計本來就該有相同的暫存器數，這個一致性正是修正正確的證據。影響：目前沒有已發表的數字用到它（`check_paper_numbers.py` 用的是 M5 的 B0），但「clock gating 省下的是每個 flop 的回授 mux」這個機制結論正是靠 edfxtp → dfxtp + ICG 的消長佐證的，漏掉 edfxtp 等於把唯一能撐住它的統計量歸零，而且不會有任何錯誤訊息。M14/M15 一旦引用 flop 數就會踩到。

- `2026-08-01-04` test — **控制路徑第一次被驗證，gate 數 36 → 37。** C2 把資料路徑驗到 2.47 億個 stage 零 mismatch，但**所有** testbench 都用同一種方式驅動 DUT：`in_valid` 連續拉高 T 個 cycle、只在 frame 開頭 reset、frame 之間必 reset。於是四件事從來沒有被激勵過。新增 `tb/cocotb/test_ctrl.py` 與 Tier A 的 `MODE=ctrl`，接成 gate `M3-1`（零容忍）：**(a) stall** —— frame 中途拉低 `in_valid` 32 個空拍，解碼位元必須與無 stall 時**逐位元相同**、`stage_done` 總數不變。`rtl/ctrl.sv` 的 `stage_en = (st == S_RUN) && in_valid` 同時 gate 住三個模組的四組暫存器（`acs_array` 的 `pm` 與 `surv_r`、`traceback` 的 `re`、`viterbi_top` 的 `bm_r`），在 `rtl_lowpower/` 它更是 ICG 的 enable —— 任何一組錯拍，C2 都看不到，因為 C2 從不 stall。**(b) `frame_done`** —— 它是 `viterbi_top` 的輸出、被 dbg wrapper 拉出來、在 GL TB 裡接了線，但 grep 全 repo **沒有任何測試讀過它**；它可以恆為 0 而所有 gate 照樣綠。現在斷言它恰好拉高一次（實測確實如此）。**(c) 幀中 reset** —— 餵到一半按下 reset 再重灌一整幀，結果必須與基準逐位元相同。這正是 `rtl_lowpower/` 的 reset-in-enable 改寫要保護的東西（`2026-07-29-17`），卻一直沒有測試碰過。**(d) 背靠背 frame** —— 不 reset 餵第二幀必須無輸出。這**斷言的是設計限制**（`ctrl.sv` 停在 `S_DONE` 直到 reset），先前沒有寫在任何地方也沒有測試保護；行為若改變這裡會叫，那時該同步更新 `docs/report.md` §5 的限制清單，而不是讓一個未記載的行為悄悄變成相依。四條全過。連動更新：`gates.csv` 36 → 37 列、有判準的 gate 30 → 31（觀測仍 6），`check_paper_numbers.py` 四處、`docs/report.md` §1、README 各一處同步；assertions 247 / mismatches 0、變異 8/8。

- `2026-08-01-05` test — **`rtl_lowpower/` 第一次被驗證，gate 數 37 → 38。** 它是 M9 的 B0′/B1′ 所合成的原始碼——也就是 **−42.7% 功耗與 −11.02% 面積這些已發表數字的來源**——但先前：不被 `check_rtl.sh` lint（只列了 `rtl/`）、不進 Tier A（`_run_group.py` 寫死 `rtl/`）、不進 Tier B（`tier_b.py` 同）。它唯一的檢查是閘級的 `ppa/verify_cg.py`，**而那道檢查在 M10 之前還驗錯了 netlist**。也就是說「兩份 RTL 語意等價」這句話一直只是註解。三項修正：(a) `check_rtl.sh` 改吃目錄清單（預設 `rtl rtl_lowpower`），兩個目錄 × 三個前端（Verilator / Icarus / Yosys 0.64）**六項全過**；(b) `_run_group.py` 加 `FEC_RTL_DIR`，Tier A 新增 `MODE=c2lp`，用同一組凍結向量、同一個 golden model 對 `rtl_lowpower/` 比對 `bm`/`pm`/`survivor`/解碼位元——**12 frames / 3144 stages，0 mismatch**；(c) 接成零容忍的 gate `M3-2`。`docs/lowpower_baseline.md` §4.1 要求「先過 C2 才准量功耗」，先前只有閘級那一半，RTL 層是空的，現在補上。連動：`gates.csv` 37 → 38、有判準的 gate 31 → 32、M3 6 → 7，`check_paper_numbers.py` 五處與 `report.md` §1、README、thesis §5.1 與附錄 D 同步；assertions 247 / mismatches 0、變異 8/8。

- `2026-08-01-06` debug — **閘級 TB 的靜默截斷改為 `$fatal`，並把 C2′ 裡那份 ACS 重寫釘回 golden。** (a) `tb/gl/tb_viterbi_file.sv` 讀激勵與期望位元時以 `i < MAXS` / `i < MAXB`（各 64K）為界，**讀滿就停、不報任何訊息**；超過的話後面的 stage 會讀到未初始化的內容，症狀是 C2 在某個 frame 之後開始 mismatch，而診斷會指向 RTL 而不是 TB。改為讀完後若未到檔尾就 `$fatal`。G7 與閘級 C2 照常通過。(b) `sweep/test_c2prime.py` 為了數平手次數而重寫了一份 ACS 前向遞迴（golden / torch / RTL 之外的第四份）。它一旦與真正的遞迴漂移，會**繼續回報一個看起來合理的平手數**而測試照樣綠——「平手真的被測到」這個保證就變成假的。改為以 golden 的 `pm` 歷史釘住它（最終 `pm` 必須逐元素相同）；實測把 tie-break 方向改掉會立刻失敗，這道釘子有牙。
- `2026-08-01-07` audit — **`bm_r` 不是 debug-only 的免費品：它真的被合成進出貨的 netlist，佔 0.065% 面積。移除它的代價不值得，改為如實記載。** `rtl/viterbi_top.sv` 的 `bm_r` 只有 `tb/dbg/viterbi_dbg.sv` 透過 XMR 讀得到，合成後的設計裡沒有任何 reader，所以原本預期 Yosys 會整個移除它、加上 `` `ifndef SYNTHESIS `` 只是把意圖寫成明文。**實測推翻了這個預期**：加上守衛之後 Q3_W8_D32 的面積從 157527.33 掉到 157424.73 µm²（**−0.065%**，cells 3738 → 3671），netlist 不再逐位元組相同。也就是說 M5 已發表的面積、功耗與 d\* 裡都含著這個只為 debug 存在的暫存器。**裁定：還原 RTL，不做這個改動。** 理由是 0.065% 的正確性換不到重跑整條 M5 + M9 量測鏈、重新裁決 F1/F2/F3、並更新所有已發表數字的代價；`docs/lowpower_baseline.md` §4.3 也明訂 B0 的既有數字不重算。還原後合成結果與先前逐位元組相同（157527.3312 µm²）。這一條記在這裡，是為了讓「published 面積含一個 debug 暫存器」這件事有出處，而不是留在某次對話裡。

- `2026-08-01-08` refactor — **設計組態與 RTL 清單單一來源化，而且沒有抹平「順序是有意義的」這件事。** 4 組 winner 先前在 `ppa/synth.py`、`ppa/sta.py`、`ppa/run_power.py`、`scripts/tier_b.py`、`scripts/m9_sweep.py` 各有一份字面值；7 個 RTL 模組的清單在 5 處。加一個模組要改 5 個地方，而其中只有 2 個會大聲失敗。新增 `scripts/design.py`。**關鍵是不能只把它們合成一份清單**：`ppa/synth.py` 與 `scripts/tier_b.py` 的迭代順序是 `(6,12,64)` 起，`ppa/sta.py`／`run_power.py`／`m9_sweep.py` 是 `(4,10,64)` 起，而 `data/results_m5_fmax.csv` 的列序就是後者——統一順序會改變已提交 CSV 的列序、**破壞冷跑的逐位元組判準**，而那是本專案最強的一條檢查。所以單一來源的是**組態的定義**（Q, W, D, clip），順序由各消費端明文宣告（`ORDER_SYNTH` / `ORDER_POWER`），並在檔內註明改順序就要面對 CSV 會變。新增 `tests/test_design_single_source.py`（10 條）：兩支無法 import Python 的 shell script（`check_rtl.sh`、`g7_icarus.sh`）仍自己列模組，但被測試釘住必須與 `design.MODULES` 一致；Python 消費端不得再出現四組 winner 的字面值；兩種順序必須是同一集合的排列；`design.WINNERS` 必須等於 `data/m2_winners.csv` 選出來的那四組。實測：38 個 gate 全綠、`data/` 非 meta 差異 **0**，逐位元組不變。

- `2026-08-01-09` add — **環境從「works on my machine」變成可釘、可驗、可攜。** 四件事。(a) **commsim 的硬編路徑拿掉了。** `golden/ref_float.py` 與 `tests/test_golden.py` 各自寫死 `/mnt/c/Users/fartw/OneDrive/Desktop/github/communications relay simulator`，而它提供 M1/M2 每一個 BER 點的 AWGN、BPSK、G1 的 Q 函數 oracle 與 cluster-robust CI —— **沒有它一個數字都重生不出來**，卻不在 `requirements.txt`、沒有版本記錄。新增 `scripts/check_commsim.py` 當唯一的定位器與內容鎖：`third_party/commsim.lock` 記下 remote、commit（`519e886`）與 7 個被用到的模組的 SHA-256。**刻意不 vendor**——G1 的價值在於它是**獨立實作**的第二意見，複製進來就不再獨立。`COMMSIM_PATH` 一旦設了就是權威的（設錯不回退到候選路徑：用錯來源比找不到更糟）。(b) **`openroad/orfs:latest` 釘成 digest** `sha256:a3e79375…`。整條 PPA（面積、Fmax、SAIF 功耗、d\*）都由那個 image 裡的 Yosys/OpenROAD/OpenSTA/sky130hd 產生，`:latest` 被上游推新版就會讓所有已發表數字改變，而 repo 裡沒有東西說得出當時用的是哪一版。`scripts/setup_eda.sh` 同理，從查 `releases/latest` 改為釘 release tag。(c) **metadata 補完 CLAUDE.md §5.3**：新增 `git_commit_golden` / `git_commit_rtl` / `git_commit_rtl_lowpower`（C2 的意義建立在兩份實作獨立，所以要各自可追溯）、cocotb / torch / CUDA / GPU 型號、容器內的 Yosys/OpenROAD/OpenSTA 版本與 ORFS image digest；`git_dirty` 改為 `git_dirty_src`（只看源碼路徑）——原本七份 meta 全記 `true`，根因是冷跑第 2 步刪掉了追蹤中的 `data/`，**恆為 true 的旗標等於沒有旗標**。(d) 新增 `.github/workflows/ci.yml` 與 `pyproject.toml`。CI **只跑免費 runner 做得到的**（pytest、數字稽核、變異測試、兩目錄的 Verilator lint、凍結本體不可變、ruff、shellcheck），並在檔頭明列**不涵蓋**的部分（GPU 掃描、Docker PPA、閘級模擬、Tier A/B、冷跑）——一個不涵蓋主要證據鏈的綠燈，本身就是誤導。commsim 是 private repo，CI 上取不到，`tests/test_golden.py` 會整個模組 skip 並把理由印出來（`-rs`），不用 try/except 靜靜吞掉。
- `2026-08-01-10` debug — **ruff 第一次跑就在檢查器自己身上抓到一個真的 bug：`fails` 在定義之前就被使用。** `scripts/check_paper_numbers.py` 的 §3.2.1 區間檢查在 324/332/335/352/406/434 行 `fails.append(...)`，而 `fails = []` 在 **592 行**。這六處全在「檢查失敗」的分支裡，所以全綠時走不到 —— 但**只要其中任何一條真的失敗，checker 就會拋 `NameError` 而不是印出那條精心寫好的錯誤訊息，而且它後面的檢查全部不會跑**。實測確認：強制觸發其中一條，得到 `NameError: name 'fails' is not defined`；把定義搬到第一次使用之前後，同樣的觸發正確印出三條 MISMATCH。**一個只在失敗時才會壞掉的錯誤處理路徑，等於沒有錯誤處理。** 另清掉 43 處 ruff 發現（未使用的 import、沒有插值的 f-string、死區域變數），並把規則集從「全開」收斂為 `E4/E7/E9/F`：全開會得到 309 條，其中 140 條是「多餘的 noqa」這類風格噪音，把那些變成紅燈只會訓練人忽略紅燈。
- `2026-08-01-11` debug — **`pyproject.toml` 的 `addopts = "-q -rs"` 讓 C2′ 這道 gate 判失敗。** `scripts/m2_gate.py` 自己會加 `-q`，兩個疊起來是 `-qq`（雙重安靜），pytest 連 `N passed` 那行摘要都不印 —— 而 C2′ 正是靠解析那行裁決的，於是 `n_pass` 解析成 0、gate 紅燈。**這是新加的設定檔在既有 gate 上造成的迴歸，而它 fail-closed（判失敗）而不是靜默通過**，正是 `gates.py` 那條「半綠的資料比沒有資料更危險」的設計在起作用。修法：`addopts` 只留 `-rs`。

- `2026-08-01-12` audit — **B2 不是 traceback 模組的 drop-in 替換，它會改動 frame 層的輸出時序。這件事 `docs/lowpower_baseline.md` 與 `docs/memory_traceback_baseline.md` 都低估了，如實記錄。** 實作 RTL 之前先對介面做的檢查發現：register exchange 在時間 t 輸出 stage `t-D+1` 的位元，而 batch 回溯在時間 t 輸出的是 stage `t-2D+1 .. t-D` **一整批**——比前者多了 **D 個 stage 的延遲**，而且是「每 D 個 stage 吐 D 個位元」而非「每 stage 吐 1 個」。但 `rtl_lowpower/ctrl.sv:59` 的 `out_valid = (stage_done && (t_done >= D-1)) || flush_en` 把輸出時序**寫死在深度 D 上**，`S_FLUSH` 的長度也是 D-1。因此 B2 需要同時改 `ctrl.sv`（門檻改 2D-1、flush 長度、以及一個 D 位元的輸出緩衝），而不只是換掉 `traceback.sv`。`lowpower_baseline.md` §2 把 B2 寫成「B1 + memory traceback」，`memory_traceback_baseline.md` §6.4 也只寫「由 `TB_MODE` 參數選擇」——兩份文件都把它當成模組層的替換。**這不改變 B2 的判準與事前預測**（它們談的是功耗與面積，與時序無關），但它把工作量從「加一個模組」變成「加一個模組 + 改狀態機 + 重驗控制路徑」，而控制路徑的四條測試（gate `M3-1`）正好是 M12 才補上的。記在這裡，讓下一次開工的人不用重新發現一遍。

- `2026-08-01-13` proofread — **M11/M12/M13 補進 README 與規格書 §9，M14 如實標為進行中。** 這三個里程碑先前只存在於 CHANGELOG——**而那正是 M11 存在要修的那個病**（M8/M9 做完了卻沒進任何對外文件，README 的進度表停在 M7）。把它留著等於重犯一次剛修好的錯。README 進度表補四列、規格書 §9 補四列，內容包含各自最重要的發現：M11 的預先登記配對重構讓 M9 第一次被驗出「時間戳只支持 60 秒」；M12 的 `ppa/` parser 測試抓到 `_dff` 漏算 enable-FF；M13 的 ruff 首次執行即在檢查器自身抓到 `fails` 在定義前被使用。**M14 標為「進行中」而不是省略**：凍結文件與 golden 的 `mode='batch'` 已完成並通過凍結的交叉檢查（C-B1 實測 0.0166%，門檻 1%），但 RTL 未做，指向 `docs/m14_implementation_notes.md`。一個做了一半的里程碑要嘛如實標示，要嘛就不該出現在進度表上——不能讓讀者從「表上沒有」推論成「還沒開始」。

## 2026-07-29（M9 起步：clock gating 的流程打通與一個被 C2 擋下的隱蔽 bug）

- `2026-07-29-15` plan — **`docs/lowpower_baseline.md`（凍結文件，量測前）**：定義三態基準線（B0 現況 / B1 +clock gating / B2 +memory traceback）、null 存活的判準（總功耗全距 < 2% **且** R² < 0.5），以及事前預測。預測不是猜的，是從 `rtl/ctrl.sv` 讀出來的：`stage_en` 在量測窗內的 duty cycle 是 **94.1%**（Q4 W10 D64：1030/(1030+64)），故 **P1 預測 clock gating 的總功耗降幅 < 10%、P2 預測 null 存活**；真正的樞紐是 B2（register exchange 每 stage 改寫 64×D 個 flop，記憶體式回溯每 stage 只寫 64 bits，那是數量級差別）。另明訂：M5 的功耗是單一 seed / 3 frames，SAIF 層級沒有 null 分布，**M9 必須補 ≥ 8 個獨立 seed 才能誠實套用 2% 門檻**。
- `2026-07-29-16` add — **`ppa/synth.py` 接上 Yosys `clockgate`**（ICG = `sky130_fd_sc_hd__dlclkp_1`，GATE:CLK:GCLK，由 liberty 實地查得而非抄文件），`clock_gating` / `rtl_dir` / `tag_suffix` 三個參數讓 B0 的既有 netlist、快取與數字完全不受影響。
- `2026-07-29-17` debug — **C2 抓到 clock gating 靜默毀掉同步 reset。** Yosys 從 FF 的 CE 腳推導 enable，而**同步 reset 不在 CE 裡**（被折進 D 路徑，netlist 是 `dfxtp` + reset mux）⇒ `rst=1` 而 `stage_en=0` 時時脈被關掉、**reset 永遠進不去**、設計卡在 X。**症狀極隱蔽**：`tb_viterbi_file.sv` 只在 `out_valid === 1'b1` 時檢查 X/Z，而 `out_valid` 自己就是 X ⇒ 回報「收到 0 個輸出」而不是「X 錯誤」。**一個功能完全壞掉的 netlist 照樣會產生 SAIF、照樣會被 OpenSTA 算出漂亮的功耗數字**——`docs/lowpower_baseline.md` §4.1「先過 C2 才准量功耗」的硬性順序當場兌現。新增 `ppa/verify_cg.py` 把這道檢查自動化。
- `2026-07-29-18` add — **`rtl_lowpower/`：可 clock gate 的 RTL 變體**（`if (rst || en) x <= rst ? RESET_VAL : d;`，三個檔案、語意等價、reset 兩種寫法都有優先權）。`ctrl.sv` 刻意不改，改以 `CG_MIN_NET = 64` 把控制 FSM（數十個 flop、gate 了也省不到功耗、且是卡死的來源）排除在 gating 之外。**`rtl/` 完全不動**，M3 的驗證與 M5 的已發表數字不需重算。
- `2026-07-30-05` result — **M9 的最強一條，而且是圖畫出來才看見的：三態的起伏形狀完全相同（r > 0.99）。** 正規化後的三條曲線在每個 SNR 點的起伏**方向與相對大小幾乎一致**：B0~B0′ r = **+0.9925**、B0~B1′ **+0.9898**、B0′~B1′ **+0.9997**。三個設計裡有一個功耗少 42.8%、RTL 寫法不同、還多了 6 個 ICG——它們唯一的共同點是**每個 SNR 點用同一個激勵 seed**。若起伏來自 SNR，它應隨資料路徑改變；若來自「抽到哪組資料」，就會像這樣被三個設計一起複製。**實測是後者** ⇒ 那條「功耗 vs SNR 曲線」的形狀是**激勵的指紋**，不是 SNR 的函數。與 M9-7 互相獨立、結論一致。
- `2026-07-30-04` audit — **自我更正：撤回「SNR 依賴是絕對量」這條歸因。** 第一版 M9-7 用「相對全距從 0.91% 漲到 1.47%」當證據。等 B1′ 的 null 分布也跑完後發現**純 seed 的相對全距用同一個倍數在漲**（0.941% → 1.423%）——相對全距放大只反映**分母縮小**，不能當「SNR 依賴」的證據。改為資料真正支持、而且更強的一條：**在每個態內，跨 SNR 的變異都等於純 seed 的變異**（B0′ 比值 **0.971**、B1′ **1.035**），而這個相等關係在總功耗砍掉 42.8% 之後**依然成立**。附帶的機制證據：資料相依的切換變異是絕對量，clock gating 幾乎動不到（跨 SNR −7.6%、seed −13.3%，對比總功耗 −42.8%）。
- `2026-07-30-03` debug — **`scripts/gates.py` 的 stale 列破口修根因。** `finalize()` 原本以 `(milestone, gate)` 為鍵**逐列**取代，於是 gate **改名**後舊列不會被取代、留下孤兒把總數灌大。這個破口 `2026-07-16-08` 咬過一次（M2 的 C2′ 改名把 26 灌成 27），M9 開發期間又咬一次（M9-7 改名）。**同一個破口咬兩次就該修根因**：本專案每個 milestone 就是一支 script、產生該 milestone 的完整集合，所以改為**以 milestone 為單位整批取代** ⇒ 孤兒在結構上不可能存在，不必再靠人發現。gates.csv 現為 **36 筆 = 30 個有判準的 gate + 6 筆觀測**（M0 3 / M1 6 / M2 3 / M3 5 / M4 3 / M5 8 / M9 8），全綠。
- `2026-07-30-01` test — **null 分布補上了（8 個獨立 seed、同一個 SNR），這是嚴審列為 S-2 的缺口。** B0′ @ 3 dB、8 個獨立激勵 seed：σ_null = **0.1415 mW**、全距 **0.941%**。而跨 SNR（1→5 dB，各 1 個 seed）的全距是 **0.914%**。**兩者幾乎一樣大**——把 SNR 固定住、只換隨機資料，散布就已經有這麼大。M5 的收斂測試（1/2/3 frames）改變的是同一段激勵的長度、不是獨立重複，所以它從來證明不了「1.0% 是雜訊」；現在有了。
- `2026-07-30-02` result — **斜率檢定：`t = +1.82`，與零無法區分，但也不宜說成「效應不存在」。** `σ_slope = σ_null/√Sxx = 0.1415/√10 = 0.04475`，斜率 `+0.0814 mW/dB` ⇒ `t = 1.82`、95% 區間 **[−0.0081, +0.1710] mW/dB** 跨過零。**正確的敘述是「未解析」，不是「不存在」**：機制本身就預測 `pm` 累加器會貢獻一個小的正斜率（golden 側 `pm` 的 R² = 0.913），而 5 個點加上 0.14 mW 的量測精度解析不出它。`t = 1.82` 恰好就是「真實但微小的效應」該有的樣子。⇒ `docs/report.md` §4 的「這條曲線不存在」語氣強於資料所能支撐，待改為「效應被侷限在累加器、僅佔約 1% 總功耗，本量測解析不出」。
- `2026-07-29-20` result — **M9 功耗實測（16 個閘級點，annotation 全部 100%）：clock gating 讓總功耗掉 42.7%，我登記的 P1 錯得很離譜。** 事前登記 P1 是「降幅 < 10%」，推理依據是 `stage_en` 的 duty cycle 有 94.1%、沒什麼可關。**推理錯在機制**：省下的不是閒置週期的時脈，而是**每個 flop 的回授 mux**（cell 數降 36.5%）。Q4 W10 D64 @3 dB：總計 44.143 → 25.275 mW（−42.7%）、traceback 29.465 → 12.222（**−58.5%**）、ACS 11.215 → 9.586（−14.5%）、min-PM 3.037 → 3.041（**+0.1%，純組合邏輯，沒有 flop 可 gate**）。traceback 的功耗佔比 66.7% → 48.4%。**三條可檢驗的事前預測，兩條被推翻（P1 降幅、P3 面積方向），一條未解析（P2）。**
- `2026-07-29-21` result — **機制的量化預測命中：SNR 依賴是一個「絕對量」，clock gating 動不到它。** 若 SNR 依賴真的只住在 `pm` 累加器（報告 §4.1），則縮小分母時它的**絕對**大小應該不變、**相對**佔比應等比放大。實測：總功耗降 42.8%，而 SNR 驅動的全距只從 **0.4043 mW 降到 0.3734 mW（−7.6%）**；相對全距 0.91% → **1.47%**，機制預測值為 0.91%/0.5716 = **1.59%**（差 8%）。**這是白化機制第一次在「改變設計」的條件下被量化驗證**，先前只有跨層（golden vs SAIF）的靜態吻合。
- `2026-07-29-22` audit — **依 `docs/lowpower_baseline.md` §3 的凍結判準，null 的裁決是「未解析」，不得宣稱任一方向。** B1′ 全距 1.47%（< 2% 門檻）但 R² = 0.568（≥ 0.5 門檻）⇒ 正好落在判準表的第三列。而且**判準本身還不能誠實套用**：同一份文件 §3 要求先以 ≥ 8 個獨立 seed 建立 null 分布，那一步尚未做。佐證這個保留是必要的——控制組 B0′ 的 R² 是 0.551、已發表的 B0 是 0.478，**同一個設計、同一組激勵，R² 就跨過了 0.5 的門檻**：5 個點根本解析不出 R²。
- `2026-07-29-19` result — **面積的兩因子拆解（Q3 W8 D32，三態 C2 全部通過）**：B0（原 RTL、無 CG）157,527 µm²；**B0′（改寫 RTL、無 CG）163,888 µm²，+4.04%**；B1′（改寫 RTL + CG）140,166 µm²，**−11.02% vs B0、−14.47% vs B0′**。**RTL 改寫本身要付 4.04% 的面積，那是必須控制的混淆因子**——沒有 B0′ 這一欄就會把 −11.02% 全歸給 clock gating。純 clock gating 的效果是 −14.47%。四個組態的 B1（未拆解版）面積降幅一致：−9.35% 到 −12.10%，cell 數降 27.9–36.5%，各 6 個 ICG。**這也推翻了我自己登記的 P3（「面積可能上升」）**——ICG 換掉的 enable 邏輯比 ICG 本身大得多。

## 2026-07-29（投稿前嚴審：文獻定位與誠信修補 M8）

- `2026-07-29-01` audit — **以審稿人視角全面覆核，找到六項規格書驗收條件被靜默放掉。** 最嚴重的是規格書 §9 M6 明列的「與既有通訊模擬器的 Pareto 前緣圖」從未產出、也從未記為未做（全 repo 搜 `pareto` 只命中規格書那一行）；`data/results.csv` 不存在，導致 `energy_model.md` §5（分開記錄 `e_dyn_per_bit`/`p_leak`）、§7（d\* 可由 results.csv 重算）與規格書 §11.4 三處承諾同時落空；`energy_model.md` §7 宣稱的 `scripts/energy_model.py` 單元測試不存在；§2 宣告要掃的 F ∈ {3,6,10} dB 與 P_circuit ∈ {20,60,120} mW 從未掃；Tier A 實跑 `FEC_NINFO=256`（22,532/86 = 262）與規格書 §3 凍結的「frame ≥ 1024 info bits」不符且未揭露。
- `2026-07-29-02` audit — **文獻檢索推翻兩項定位。** (a) 臨界距離有直接前案：Howard/Schlegel/Iniewski, EURASIP JWCN 2006（doi 10.1155/WCN/2006/74812）推導 `d_CR` 並涵蓋**多個解碼器實作（含類比）、跨環境、跨寬頻率範圍**，覆蓋面廣於本專案的單一組態 —— d\* 確定不能當頭條，須降為 motivation 並以該文為對照。(b) 「固定排程 Viterbi 的功耗與 SNR 幾乎無關」是 adaptive-decoding 文獻的**背景常識**（前案原文：decoding effort "independent of SNR and fidelity requirements"），故論文主張必須從「我們發現 X」改為「X 已知，本文給出機制、可計算的界與功耗簽核的流程後果」；同時證實 report §4.4 未範圍化的推廣是錯的（adaptive / T-algorithm 類低雜訊下省電達 97%，其活動依定義隨 SNR 變）。
- `2026-07-29-03` add — **補上 `data/results.csv`（8 列），分開記錄 `e_dyn_per_bit_j` 與 `p_leak_w`**，兌現 `energy_model.md` §5 的承諾：`E_dec(f_clk) = e_dyn_per_bit + p_leak / f_clk` 使任何時脈下的 E_dec 都可重算，不必重跑 gate-level 流程。M5 六個 gate 全綠，d\* 與 F1–F3 的數字逐一不變（最小 d\* 17.8 m、Δd\* +11.29%/+6.31%/−0.75%/−0.43%）。
- `2026-07-29-04` add — **`check_paper_numbers.py` §4d：32 個 d\* 全部由 `results.csv` + 凍結常數現算後與 `results_m5_dstar.csv` 逐列對帳**，兌現 `energy_model.md` §7 的承諾。先前所有 d\* assertion 走的是「讀已算好的 `dstar_m`」，驗的只是報告有沒有抄對 CSV，不是 CSV 算得對不對。變異測試證實新檢查有鑑別力：把 `e_dyn_per_bit_j` 改 10% 觸發 8 條、把一個從未被報告引用的 `dstar_m`（η=0.5）改成 999.99 觸發 1 條 —— **後者舊版檢查器完全看不見**。assertion 仍 190 條、mismatches: 0。
- `2026-07-29-08` add — **不確定度傳播打通：BER 的 cluster-robust CI 一路傳到 required Eb/N0、d\*、Δd\*。** 先前整條鏈路的區間**到 BER 表就斷了**，於是 17.8 m、+11.29%、−0.75% 全是裸點估計——而 F2 的門檻是 5%、F3 看的是符號，兩者都必須有區間才判得準。新增 `golden/ber.py` 的 `required_ebn0_ci`（參數化 bootstrap，固定 seed；刻意不用解析偏微分，因為那等於把 `ebn0_at_target_ber` 再實作一次、分岔時不會有錯誤訊息）與 `scripts/energy_model.py` 的 `d_star_ci` / `delta_dstar_ci`（未編碼 Eb/N0 為**共模項**，兩組態共用同一抽樣值，各抽各的會高估 Δd\*）。未編碼的 σ 由 `results_m1.csv` **現算**而非寫死。
- `2026-07-29-09` result — **裁決是被解析出來的，不是雜訊。** required Eb/N0 的 σ = 0.0208–0.0239 dB。**四個 Δd\* 的 95% 區間全部不跨過零點**：模型 A +11.29% [+10.84, +11.75] / +6.31% [+6.06, +6.55]（兩者完全在 5% 門檻之外）、模型 B −0.75% [−1.05, −0.45] / −0.43% [−0.60, −0.26]。d\*(Q3 D32, A/室內) = 17.76 m [17.60, 17.91]。⇒ **F2 與 F3（含自稱最咬得住的「符號會翻轉」）的裁決穩健**，這是先前無法宣稱的事。反面同樣被解析出來：三個 winner 的 required Eb/N0 為 4.1522 / 4.1909 / 4.1937 dB 而 σ 是 0.0239 / 0.0225 / 0.0208 dB ⇒ **它們的排序統計上是平手**，「BER 最佳」只在點估計意義上成立。`check_paper_numbers.py` 新增硬性檢查：任一 Δd\* 區間跨零就紅燈，報告不得再宣稱符號翻轉。211 條 assertion、mismatches: 0、變異測試 6/6。
- `2026-07-29-10` add — **補跑 `docs/energy_model.md` §2 宣告過、但 M5 從未執行的兩條掃描**（F ∈ {3,6,10} dB、P_circuit ∈ {20,60,120} mW），產出 `data/results_m5_sensitivity.csv`（288 個組合）。d\*(Q3 D32, η=0.1)：NF 3/6/10 dB 給 216.9 / 153.6 / 96.9 m（模型 A 自由空間）與 21.6 / 17.8 / 13.6 m（室內）；P_circuit 20/60/120 mW 給 63.0 / 86.0 / 104.8 m（模型 B 室內）。新增 gate **F1b**：規格書 §0 的證偽條件寫的是「在合理參數範圍內」，但 F1 原本只在預設參數點上裁決——比條件本身要求的弱。掃完 288 個組合後最小 d\* = **13.65 m**（NF=10 dB、P_c=20 mW）仍遠大於 1 m ⇒ **F1 的存活比先前更強**。
- `2026-07-29-11` audit — **`docs/energy_model.md` §5 的 power gating 量級寫錯了，實測更正。** 原文說不做 power gating 會把 E_dec「灌大約 200 倍」，並要求報告必須明講這個假設。**200 倍是漏電那一「項」的比值**（`(1/R_info)/(1/f_clk)` = 2e-6/1e-8），不是 E_dec 的比值；而漏電只佔本設計總功耗的 **0.00028%**（64 nW vs 24 mW）。實測（新增 gate M5-4，288 個組合取最大）：**E_dec 只變 0.055%、d\* 只變 0.027%** ⇒ **這個假設其實無關緊要**。對報告是好消息（少一條 caveat），但文件的量級不得沿用。`energy_model.e_dec_of()` 同時實作兩種情境並附方向性斷言（關掉 gating 只能讓 E_dec 變大）。gate 數 26 → 28，assertion 211 → 224，mismatches: 0。
- `2026-07-29-12` add — **補上規格書 §9 M6 唯一被靜默放掉的驗收條件：Pareto 前緣圖**（`scripts/plot_pareto.py` → `figures/fig_pareto.png`，已接進 `make figures`）。分兩層並明確標示哪層是量測：左圖以 survivor 記憶體 64×3D 位元為**面積代理**涵蓋全部 64 個網格點（附 95% CI 誤差棒），右圖以 gate-level SAIF **量測**的 E_dec 涵蓋僅有的 4 個合成組態。代理前緣全由 Q=6 構成（Q6/D64 4.152、D48 4.175、D32 4.194、D24 4.446 dB），量測前緣則含 Q4 與 Q3（470 / 441 / 303 / 241 pJ）。兩軸刻意不合併：代理指標不是能量，而面積與能量在本設計裡並非同一件事。
- `2026-07-29-13` audit — **補揭露兩項規格偏離與一項覆蓋界線。** (a) Tier A 的 frame 長度是 **256**，非規格書 §3 凍結的「≥ 1024 info bits」——M1 凍結向量本身即為 256-bit。Tier B 用的是 1024（20,000 × 1,024 bits，0 mismatch），故「≥ 1024」在**解碼位元**層級已涵蓋，未涵蓋的是 1024-bit frame 下的 per-stage `bm`/`pm`/`survivor` 比對。**刻意不重新產生凍結向量來消除這項不符**：向量已在 tag `m1-golden` 凍結並帶 SHA-256，為了讓事後發現的不符看起來消失而改動凍結物會毀掉凍結的意義（CLAUDE.md §5.1）。(b) **只有 4 個組態被合成過，Q=5 從未合成**，故「d\* vs Q」在 D=32 上只有兩個資料點，中間是連線不是擬合。(c) 本報告不含任何 baseline 比較表；直接前案 Howard/Schlegel/Iniewski 2006 的覆蓋面廣於本報告。
- `2026-07-29-14` audit — **把「gate」與「觀測」分開計數**：`gates.csv` 的 28 筆記錄裡有 **26 個有 pass/fail 判準的 gate + 2 筆觀測**（M5-3 功耗 vs SNR、M5-4 power gating，兩者的 expected 欄本就自陳「觀測，不是 pass/fail」）。混報成「28 個 gate 全綠」是灌水——沒有判準的東西不可能不綠。`check_paper_numbers.py` 把分類釘成硬性檢查，防止它悄悄漂移。**（自我更正：先前嚴審時說「M2 全網格、M5-2、M5-3 三個沒有判準」是錯的——M2 全網格的判準是 64/64、M5-2 是 < 5% 變動，兩者都是真判準。實際只有 2 筆。）**
- `2026-07-29-06` audit — **自我更正 report §5-1 / thesis §7.3-1 一個方向反了的推論。** 原文宣稱「d\* 的絕對值是上界，但 Q 之間的**相對**比較不受影響（同一 D 下 traceback 完全相同，實測差 0.08%）」。實際上 Δd\* 由 `Δln(E_dec)` 驅動，而 traceback 正是 E_dec 裡**與 Q 無關**的那一項；高估它會把 `E_dec(Q6)/E_dec(Q3)` 往 1 拉，即**稀釋** Q 依賴。「traceback 完全相同」正是它**會**影響相對比較的理由。新增 `data/results_m5_tb_sensitivity.csv` 敏感度線（非量測值）：traceback 功耗係數 1.0 / 0.5 / 0.2 / 0.1 對應 E_dec 比值 1.2586 / 1.3551 / 1.4572 / 1.5056、Δd\*（模型 A / 室內）+6.31% / +8.57% / +10.85% / +11.89%。**結論：d\* 的絕對值是上界、Δd\* 是下界，兩個偏差方向相反**；F2 與 F3 的裁決均不受影響。12 條新 assertion 加一條單調性斷言把該表釘回 CSV。
- `2026-07-29-07` audit — **撤回兩處資料撐不住的宣稱。** (a) thesis §4.4 原寫「D=24 會可觀變差，**D=64 接近最佳**」，但 `d_sweep.csv` 的 windowed−ML 損失是 +0.209 / −0.050 / +0.058 / −0.072 dB（D=24/32/48/64）——**非單調且出現負損失**（窗口回溯贏過全幀 ML，數學上不可能），三者全落在 ±0.076 dB 雜訊地板內。改為只宣稱「D 低於 5K=35 會可觀變差」。(b) thesis §6.3 的 winner 排序：Q6/D64、Q4/D64、Q6/D32 三者所需 Eb/N0 全距僅 0.0415 dB，小於雜訊地板，**統計上是平手**；改為說明四個組態是依「張開 PPA 設計空間」選的，而非依 BER 排名。唯一被解析出來的 Q 效應是與 Q3 的 0.166 dB 差距——正是 Δd\* 所依據的那一項。
- `2026-07-29-05` test — **補上 `tests/test_energy_model.py`（21 條），以解析 `docs/energy_model.md` 的方式斷言常數一致**（不另抄一份數字，否則驗的只是兩份副本一致，預先登記文件就失去約束力）。第一次執行即抓到凍結文件的兩個印刷誤差：λ 印 0.124913 m 為截斷值（c/f_c = 0.12491352417，六位小數應為 **0.124914**）；L1 印 1.01174e4 而實際 (4π/λ)² = **1.012047e4**，相對差 3.04e-4。**兩者都不改變任何已發表數字**（d\* 只動 0.0152%（n=2）/ 0.0087%（n=3.5）），且程式端無須修正（它從原始常數現算，算法正確）；待修的是文件的兩個導出值，依 `energy_model.md` 標頭需另行說明理由後才可改。

## 2026-07-18（把 docs/ 整併為碩士論文草稿）

- `2026-07-18-01` add — **整併 `docs/report.md` 與五份凍結文件（trellis / traceback / wordlength / energy_model / falsification）為單一 `docs/thesis.md` 碩士論文草稿**（前置頁面 + 八章 + 參考文獻 + 附錄 A–D），接入 `figures/` 既有 9 張圖，文獻回顧與參考書目以 `[待補引用]` 骨架呈現（不捏造書目）。承重量測數字（G1 9.571 dB、編碼增益 5.434 dB、d\* 最小 17.8 m、F2 +11.29% / +6.31%、α = 0.517 等）以**獨立區塊**納入 `scripts/check_paper_numbers.py` 稽核（驗「等於 CSV 真值」+「字串出現在文中」，刻意不經過 `len(A)` / README 自我計數、不套完備覆蓋掃描）；`make report` 仍 `mismatches: 0`。**未修改任何凍結文件與 `report.md`。** 尚待使用者提供：論文題目、誌謝、英文摘要核定、文獻與參考書目之實際引用。

## 2026-07-17（完整冷跑：從零重生成為實測事實）

- `2026-07-17-01` test — **完整冷跑（`make repro`，含 GPU 的 M2 掃描）：刪光 `data/`（含 GPU 產物 `cache_m2`）從零重生。** M0–M5 全綠、GPU BER 掃描 14 分重算、gate-level 的 f2 SAIF 補回（10 個檔、488→18 MB）。**除 `data/meta_*.json`（時間戳）與 `vectors/MANIFEST.json`（`make freeze` 重寫的 metadata、凍結內容不變）外，`data/` 的每一個 CSV 與 SAIF 都逐位元組重生**——含 `gates.csv`（26 列，正準化生效）與 `results_m2`/`m2_grid`/`m2_winners` 等 GPU 產物。**規格書 §9 的「刪光 data/ 一鍵從零重生」從宣稱變成實測事實。**
- `2026-07-17-02` debug — **冷跑抓到最後一個 stale：gate 總數被硬寫成 27，真正是 26。** M2 的 C2′ gate 曾改名（`…L2-GPU…` → `…L2-torch…`），舊列在 `gates.csv` 遺留成孤兒、把總數灌成 27（見 `2026-07-16-08` 已移除孤兒列）。但 `docs/report.md`、`README.md` 與 `scripts/check_paper_numbers.py` 仍寫 27（且 report 的分解把 M2 算成 4）。這是「硬寫的期望常數」在真值改變後沒同步的經典破口——而 checker 的 gate 數 assertion（`len(GATES)` 對 27）正好抓到它。改為 26（M0 3 + M1 6 + M2 3 + M3 5 + M4 3 + M5 6），report 190/0、mutate 6/6。
- `2026-07-17-03` audit — **專案定案。** 規格書 §10 的六項 Definition of Done 全部達成並實測驗證：gate 全綠、C2 對外統計成形、BER 曲線到 1e-6 含 CI、d\* + 證偽條件裁決、`make repro` 一鍵從零重生（tag `m7-repro`）、一句話結論以真數字填在 `report.md` §6。規格書 §10 補上定案註記並指向 report §6（澄清實際掃到 Q3→Q6，非模板示意的 3→5 bit）。**如實重申邊界，不因定案而默示為更強**：d\* 的絕對值是**上界**（survivor 用 flop 陣列非 SRAM macro，高估面積/E_dec/d\*）；PPA 僅 full-parallel（PAR=32）；ADC 為敏感度線非量測；折疊架構 / post-route P&R / memory-traceback 未做——全部見 `report.md` §5。延伸方向（Polar SC 等）依 CLAUDE.md §4.3 需另行書面確認，本次不開工。

## 2026-07-16（冷跑抓到的可重生性 bug）

- `2026-07-16-01` debug — **第一次冷跑就抓到 clean 不 hermetic。** `scripts/repro.sh` 的 `rm -rf data ppa/out figures` 漏了 build 狀態，於是 `tb/cocotb/build/_passed/` 的 36 個 stale cocotb pass-marker 沒被清掉。`run_tier_a.py:94` 看到 marker 就 `continue`**跳過模擬本身**：c2 跳過卻仍計入 `C2_TOTAL` 聚合計數 ⇒ m3_gate 讀到假 PASS（**C2 根本沒重跑，是回放舊計數**）；g6neg 跳過 ⇒ 沒有新鮮的「G6 violated」證據 ⇒ 誠實地失敗。**是「g6neg 要求新鮮證據、c2 只信任聚合計數」這個非對稱，擋下了整個 M3 的假綠。** 修法（不是放寬判準）：`repro.sh` 與 `Makefile` 的 clean 都補上 `obj_dir sim_build tb/cocotb/build`，讓 clean 涵蓋所有生成狀態，M3 才會真的重建 32 個 Verilator model。
- `2026-07-16-02` debug — **`data/results_m2.csv` 的列序不確定。** `scripts/m2_gate.py` 的 `load_all()` 以 `os.listdir(CACHE)`（未排序）列舉快取、直接寫進 CSV；快取重建後檔案落地順序不同 ⇒ 280 列的**值逐位元組相同、只有列序會變**（整數 Viterbi + 固定 seed ⇒ 數字必然逐位元組相同）。而它是報告數字的單一事實來源，列序漂移會讓冷跑誤報「不重生」。修法：`load_all()` 的 listdir 排序，且寫出前依正準鍵 `(Q,clip,W,D,snr_db)` 排序。**已驗證：正準化後的 CSV 與 HEAD 排序後逐位元組相同（280 列值不變），`m2_grid/winners/floor` 不受影響、`check_paper_numbers.py` 不讀 `results_m2.csv`（M2 數字取自 `m2_grid/winners`）故稽核不受影響。**
- `2026-07-16-03` test — **GPU 被別的專案佔用，改跑 CPU 範圍的冷跑（新增 `scripts/repro_cpu.sh`）。** 全鏈路只有 M2 的 BER 掃描硬相依 GPU（`grid_runner.py` 寫死 `device="cuda"`），而它只在處理未快取的點時才碰 CUDA ⇒ **保留 `data/cache_m2/`（GPU 產物），`make m2` 直接 no-op 掉 GPU、純 CPU 走完**。於是「保留 cache_m2、其餘全刪重生」是 GPU 空出前能做的最強驗證。第一次 CPU 冷跑**每個 gate 全綠**（M1 6/6、M2 3/3、M3 5/5、M4 3/3、M5 6/6、report 190/0、mutate 6/6）——**科學結果完全重生；其中 M3 全綠（含 g6neg 4/4）直接證實 Bug A 修好**（第一次冷跑正是死在這）。但逐位元組驗證又抓到三件事（下三條）。
- `2026-07-16-04` debug — **Finding 1（Bug B 的孿生）：`data/results_m1.csv` 列序不確定。** `scripts/m1_gate.py` 的 `imap_unordered` 依 worker 完成順序寫列 ⇒ 99 列**值逐位元組相同、只有列序變**。修法：寫出前依正準鍵 `(config, ebn0_db)` 排序。**與 Bug B 同一類——這種列序不確定性在兩個里程碑各出現一次，冷跑各抓到一次。**
- `2026-07-16-05` debug — **Finding 2：`ppa/run_power.py` 的收斂點 f2 會被靜靜丟掉。** 收斂迴圈在 520s 預算用盡時 `break`，但函式**仍 `return 0`** ⇒ `power.json` 少一個收斂點（f2）卻被當成功，連帶 `saif/*_f2*.gz`、`saif/MANIFEST.sha256`、`gates.csv` 的 M5-2 全都不確定（f2 有時在、有時不在，看時間預算落在哪）。修法：`break` → `return 1`，讓 `until` 迴圈續跑補齊；`point()` 有快取，最後一趟全命中才寫出完整、確定的 `power.json`。
- `2026-07-16-07` test — **CPU 範圍的可重生性成為實測事實。** 修好 Finding 1/2/3 後跑完整 `scripts/repro_cpu.sh`（hermetic：刪光 `cache_m1`/`cache_m5` + 所有 build 狀態 + M1/M3/M4/M5 的證據與 SAIF，**保留 GPU 產物 `cache_m2`**，從零重算 59 分）：除 `data/meta_*.json`（時間戳）與 `data/gates.csv` 列序（值完全相同）外，**每一個 CSV 與 SAIF 都逐位元組重生**——`results_m1` / `m3_c2` / `tierb`(+manifests) / `results_m4` / `results_m5_*` / `results_m2` / `saif/*.saif.gz` / `MANIFEST.sha256`，且 report 190/0、mutate 6/6。**只剩 M2 的 BER 掃描（`cache_m2`）硬相依 GPU**，待 GPU 空出後由 `make repro` 坐實完整的「刪光一切從零重生」宣稱、再 tag `m7-repro`。
- `2026-07-16-08` debug — **完整冷跑（含 GPU）前的 pre-flight，抓到 `gates.csv` 的兩個問題。** (1) **stale 列**：M2 的 C2′ gate 曾改名 `…L2-GPU…` → `…L2-torch…`，而 `finalize()` 的 replace-by-key 以 gate 名為鍵，改名後舊列（24 tests）沒被取代、成了孤兒，現行 `m2_gate.py` 不再產生它。(2) **列序非正準**：M2 的列被拆成兩段（散在 M1↔M3 與 M4↔M5 之間），而完整 `make repro` 依 m0→m5 依序重建 `gates.csv`。完整冷跑對 `gates.csv` 用**嚴格逐位元組判準**（不像 `repro_cpu.sh` 豁免列序），會敗在這。修法：`scripts/gates.py` 的 `finalize()` 依 milestone **穩定排序**（列序確定、與 gate 執行順序無關），並移除 stale 列。26 列，M0..M5。
- `2026-07-16-06` refactor — **Finding 3：wall-clock 計時遙測滲進了 git 追蹤的證據檔。** `sim_khz`（`results_m4.csv`、`tierb.json`）、`gen_s`/`sim_s`（`tierb.json`）、`sim_s`/`wall_s`（`power.json`）、`gen_seconds` + `metadata`（`tierb_manifests/*.json`）本質上每次都會變。科學值（BER、mismatch=0、SHA-256、功耗 W）全部逐位元組重生，只有這些計時欄位在動。**修法（不放寬判準，而是修根因）：把計時遙測移出證據檔**——console 即時回饋保留，run 層級 metadata 本就完整記在 `data/meta_m4_tierb.json`（冷跑已豁免），per-point 那份是多餘的。改動 `tier_b.py` / `m4_gate.py` / `run_power.py` / `gen_stimulus.py`。**已用程式逐檔驗證重生的證據相對 HEAD 只差這三項修正、無任何科學值改變。**

## 2026-07-15（M6：報告 + 數字稽核）

- `2026-07-15-01` add — **`docs/report.md`**：全文的每個數字都由 `data/*.csv` 現算。寫作順序刻意是「先跑 `scripts/report_numbers.py`、再照抄它的輸出」——反過來（先寫報告再找數字對照）就是論文數字與資料脫節的標準死法。
- `2026-07-15-02` add — **移植 `check_paper_numbers.py`**（自 RISC-V 專案），並補上一個上游沒有、而本專案**最需要**的檢查：**預先登記的 commit 時間戳必須早於量測**。實測 `docs/falsification.md` / `docs/energy_model.md` 於 `ae9e151` (02:30:35) 加入，功耗量測於 `fabc105` (23:43:42) 加入——**早 21.2 小時**，git 可驗證。不機械化驗證這一條，「我們事前就登記了」就只是一句自稱。**144 條 assertion，mismatches: 0。**
- `2026-07-15-03` test — **變異測試 `scripts/mutate_check.sh`：一個抓不到錯的檢查器沒有價值。** 注入 6 種已知錯誤（改一處引用 / 改全部 / 復活已撤回的主張 / 新增未覆蓋的百分比 / 新增未覆蓋的帶單位數字 / 誤引預先登記），**6/6 全部抓到**。
- `2026-07-15-04` debug — **變異測試抓到 checker 的三個真洞（以及它自己的一個）**：(1) **coverage gap 不影響 exit code** ——上游那支工具把它當警告，於是把 `**17.8 m**` 改成 `**19.9 m**` 之後，因為 "17.8" 在結論散文裡還有一處沒被改到，「值須出現在文件中」照樣通過，**CI 會綠燈放行一個被改壞的數字**。已升級為 failure。(2) 覆蓋掃描只掃百分比，改為掃**所有帶單位的數字**。(3) `\b` 不能加在 `%` 後面（兩個非字元之間沒有 word boundary），`42.7%。` 永遠比不到——是我自己「收緊」regex 時弄壞的。(4) **變異測試自己也錯了**：原本以 `grep MISMATCH` 當判準，但報告裡本來就有一個既存的 mismatch，於是每次都 grep 得到，四個變異全部假性「通過」。改為以 **exit code** 判準，並先確認乾淨狀態 exit 0。
- `2026-07-15-05` debug — **`data/gates.csv` 有 29 列重複。** `Run.finalize()` 是 append（註解寫「保留歷史」），但列裡沒有時間戳，分不出哪一列來自哪一次 run——那不是歷史，是重複（m5_gate 跑了 6 次 → 6 份 M5 gate）。而它是報告數字的**單一事實來源**。改為以 `(milestone, gate)` 取代，檔案變成冪等；56 列 → **27 列，M0–M5 全綠**。
- `2026-07-15-06` audit — **自我更正兩處報告錯誤**（都是 checker 抓到的，不是人看出來的）：(1) min-PM 面積佔比寫成「12.0–19.7%」，真值最小是 **11.8%**；(2) min-PM/PM-regfile 倍數寫成「2.2–2.5 倍」，但真值最大 2.4479，1 位小數應是 2.4——改為 **2.21–2.45 倍**（與逐組態的表一致）。
- `2026-07-15-07` audit — **`scripts/report_numbers.py` 的趨勢檢定抓到我自己的過度宣稱**：先前把 pm 寫成「隨 SNR **單調上升**」，但實測 2 dB 有 0.3% 的凹陷，**非嚴格單調**。5 個點的「看起來單調」不是證據。改用線性迴歸的 R²——結論反而**更強**：**pm 的 R² = 0.913**，而 **`surv` 的 R² = 0.000（與 SNR 零相關）**。規格書 §7、CHANGELOG、plot 標題、m5_gate 的輸出全部同步更正，並把「pm 單調上升」列入 checker 的**已撤回主張防護**（R3），防止它悄悄復活。

## 2026-07-14（M5：PPA、能量模型、證偽裁決）

- `2026-07-14-34` add — **gate-level 功耗，8 個點，annotation coverage 全部 100%。** 階層式 Yosys（不 flatten）→ Icarus + sky130 behavioral models → `ppa/vcd2saif.py` 串流轉 SAIF（VCD 永不落地）→ OpenSTA `read_saif -scope tb_viterbi_file/dut`。收斂性實測：1/2/3 frame 分別 44.12 / 44.03 / 44.08 mW（<0.2%），不是假設。
- `2026-07-14-35` result — **三條預先登記的證偽條件全部不觸發，主張存活**（`docs/falsification.md` §5）。F1：最小 d\* = **17.8 m** ≫ 1 m。F2：模型 A 的 Δd\*(Q3→Q6) = **+11.29%**（自由空間）/ **+6.31%**（室內），> 5%。F3：符號 A 正、B 負，且 |A| < 30%。**§3.4 自稱「最咬得住」的「符號會翻轉」預測確認成立**，且模型 B 的量級幾乎完全命中（事前 −0.87/−0.50%，實測 −0.75/−0.43%）。
- `2026-07-14-36` audit — **但我的 α 點估計錯了 3.4 倍，如實記錄。** 事前登記 α ≈ 0.15，實測 **α = 0.517**。兩個錯誤都是我的：(1) 用 **flop 數**去估功耗佔比——traceback 佔 67.7–84.1% 的 flop，卻只佔 **43.0–54.2%** 的功耗（ACS/min-PM 有大量組合邏輯，燒 switching power 但不是 flop）；(2) **完全漏掉 min-PM 的 argmin 樹**——它同樣隨 W 縮放（2.482 → 4.081 mW，+64.4%，超線性）。誠實的補充：`falsification.md` §3.2 的表**本來就列了 α=0.50 那一列（+10.8/+6.1%）**，實測正好落在那裡。**公式與符號推理都通過了；被推翻的只有點估計。**
- `2026-07-14-37` result — **意外發現：min-PM 的 argmin 樹佔 12–20% 的面積，比整個 PM register file 還大。** 這是選 best-state traceback（而非 fixed-state）的直接、可量測的代價——正是當初規劃時那個問題的答案。
- `2026-07-14-38` result — **規格書 §7 的「功耗 vs SNR 依賴曲線」交付物被實測推翻**（風險 R1 成真）。總功耗在 1→5 dB 只變動 **1.0%**，**非單調**（= 雜訊），且方向與規格書前提相反。**分區塊也救不了它**：連 ACS 的 switching 都只變 1.8%。這不是「被 traceback 稀釋」，是效應**本來就不存在**。原文保留在規格書 §7 並標註為被推翻的前提。
- `2026-07-14-39` result — **把 null 變成機制（`scripts/diag_mechanism.py` + `ppa/saif_toggle.py`，兩條獨立路徑交叉驗證）。** 量化器是對稱的：`r(c=1) = (2^Q−1) − r(c=0)`，而 `2^Q−1` 是全 1 ⇒ **`r1 == ~r0`（位元互補）**。編碼位元 i.i.d. uniform ⇒ 編碼位元一翻，**r 的每個位元都跟著翻** ⇒ 翻轉率 ≈ 0.5，**與 SNR 無關、與雜訊無關**。Survivor 端同理：狀態遞迴使「正確的 survivor bit」**就是 `u[t−6]`——資訊位元本身**（以代數斷言驗證）。⇒ **一個完美的 Viterbi 解碼器與一個完全失效的，其 survivor 記憶體切換活動一模一樣。** 實測：`surv` 翻轉率變動 0.98%，而「真實路徑一致率」0.8982 → 1.0000 —— **資訊在增加，活動沒動。**
- `2026-07-14-40` test — **反事實證明機制（不只是宣稱）**：加一個整數 DC offset（＝真實 ADC 的比較器偏移）打破位元互補性，兩個 rail 在 bit1/bit2 上變成相同 ⇒ **那兩個位元的翻轉率從 0.504 崩到 0.000**，而 bit0/bit3 維持 0.502–0.504。定律 `toggle(k) = 0.5 · 1{bit k 在兩 rail 上相異}` 精確成立。**推論超出本專案：任何接收「編碼過（=白化過）」訊號的解碼器，資料路徑活動都與 SNR 無關——編碼的作用本來就是白化。**
- `2026-07-14-41` audit — **這條機制我試錯了四輪，全部保留在 `diag_mechanism.py` 的 docstring。** 第一輪 P5（「拿掉 AGC 效應就會回來」）**真的錯了**，理由（撞 rail）也錯——訊號在 ±1、A=1.78，根本碰不到 rail。第二輪 P7/P8 **又不成立**，**又是實驗設計寫壞**（在 30 dB + AGC 下取 rail，A=0.08，訊號整個被 clip，兩種量化器都塌到 {0,15}，symmetry-breaking 根本沒發生）。第三輪用 `levels=2^Q` 破壞對稱性仍失敗（rail 落在 3.493，離 round 邊界 0.007，光是 rounding 就把 bit0 隨機化）。第四輪才對。**核心主張（P1/P2/P4/P6）四輪都沒被打破。**
- `2026-07-14-42` result — **唯一真的隨 SNR 動的訊號是路徑度量 `pm`**：它是整條資料路徑上**唯一不在最大熵**的（翻轉率 0.30，其餘都 ~0.47），因為它是**累加器**。以線性迴歸的 R² 判定趨勢（5 個點的「看起來單調」不是證據）：**pm 的 R² = 0.913、全距 3.3%**，而 **`surv` 的 R² = 0.000 —— 與 SNR 零相關**（bm 0.020、re 0.247 也都無趨勢）。但 PM+min-PM 只佔 ~7–13% 功耗，稀釋到總功耗只剩 ~1%。
- `2026-07-14-42b` audit — **自我更正**：上一版把 pm 寫成「單調上升」，但實測 2 dB 有 0.3% 的凹陷，**不是嚴格單調**。是 `scripts/report_numbers.py` 的趨勢檢定抓到的（它才是報告數字的來源）。已改為 R² 的說法（結論更強：surv 的 R² 是 0.000）。規格書 §7、plot 標題、m5_gate 的輸出同步更正。
- `2026-07-14-43` test — **跨層驗證**：numpy golden（演算法）預測 survivor 翻轉率 0.4664/cycle，Sky130 gate-level SAIF（硬體）實測 0.4661/cycle，**吻合到 1% 以內**（5 個 SNR 點全部）⇒ 機制在**演算法**裡，不在 RTL 裡。另一條獨立檢查：SAIF 的 `Σ TC/cycle` 與 OpenSTA 的 switching power **逐點吻合**（含 4 dB 那個**非單調的凹陷**，兩邊都有）⇒ SAIF→OpenSTA 這條路是健全的。
- `2026-07-14-44` debug — **Fmax：純邏輯合成的 netlist 上報 Fmax 是沒有意義的。** 直接跑 OpenSTA 得到 166 ns（6 MHz），但關鍵路徑是**一顆最小尺寸的 `clkinv_1` 花了 102.6 ns**——`u_ctrl` 的 enable 直接扇出到 register exchange 的 **8683 個 sink / 18.1 pF**，中間**沒有任何 buffer tree**（下一顆閘再吃 62 ns，那是 **slew**，不是負載）。Yosys 的 `abc -liberty` 只做技術映射，**不做負載感知的 buffer 插入**。跑 OpenROAD `repair_design`（floorplan → global placement → 依 liberty 插 buffer tree）後：**6.0 → 150.2 MHz**，最大扇出 8683 → 70，面積只多 3.0%。**4 個組態全部 ≥ 101 MHz > 100 MHz ⇒ 能量模型假設的 f_clk 站得住。** 不影響已量到的功耗/面積/d\*（動態能量 `E = α·C·V²` 與頻率無關，而那個巨大的 C 本來就在 netlist 裡）。
- `2026-07-14-45` add — **ADC 能量敏感度線**（`docs/energy_model.md` §6.2 早就承諾了）：`E_ADC/info bit = 2·FoM·2^Q`。FoM 10/100/500 fJ 下，Q=6 的 ADC 佔 0.4% / 4.0% / **17.4%**，Δd\*（模型 A/室內）從 +6.31% 變成 +6.42% / +7.36% / **+11.27%**。**與 E_dec 同向 ⇒ 只會加大 Δd\*、不會翻轉符號 ⇒ F2 的結論只會更穩。** 如實標註為敏感度線、非量測值。
- `2026-07-14-46` debug — **SAIF 檔名碰撞（歸檔完整性）**：`act_<tag>_snr<snr>.saif` **沒把 frames 放進檔名**，於是收斂性用的 1/2-frame run **靜靜覆寫了主 run（3 frame）的 SAIF**。功耗數字沒錯（當下用的是正確的 SAIF），但**歸檔下來的 SAIF 與所報的功耗對不起來**——而文件宣稱 SAIF「可入庫當證據」。改成 `_f{frames}` 後重跑受影響的點，**重算的功耗與先前回報的完全一致（Δ = 0.0000%，三個點）**——順便成了一次可重生性測試。
- `2026-07-14-47` audit — **SAIF 入庫方式修正**：`.gitignore` 原本寫「SAIF 入庫（2-10 MB/點）」，**那個估計錯了 5-10 倍**（真實 netlist 有 43 萬條 net，每個 SAIF 30-56 MB，10 個共 466 MB）。但 SAIF 是純文字、結構重複，**gzip 有 27.6 倍：466 MB → 18 MB**。承諾照樣兌現，改成壓縮存放 + 未壓縮檔的 SHA-256 manifest。**已驗證：只用入庫的 `.saif.gz` 重算，結果與原始 `.saif` 逐位元組相同** ⇒ git clone 後不必重跑 gate-level 模擬就能驗算功耗證據。
- `2026-07-14-48` audit — **PPA 表如實標註「僅 full-parallel（PAR=32）」。** 規格書 §7 原本要求「三種平行度」，使用者裁定跳過折疊架構（PAR=8/1）與 post-route P&R。testbench 本來就設計成可免費複用（C2 由 `stage_done` 觸發，不是數 cycle），折疊版可隨時補上。同時未做、不得默示為已驗證：post-route P&R、memory traceback 對照、SRAM macro 版本。

## 2026-07-14（M3 + M4）

- `2026-07-14-25` add — RTL：full-parallel（32 個 radix-2 butterfly）+ **register exchange** 的 traceback。依 `docs/traceback_convention.md` 的 uniform-depth-D 語意——教科書的批次 memory traceback 有效深度落在 [D, 2D]，解碼位元會與凍結文件不同。trellis 表由 RTL 自己從八進位多項式推導，**不從 L2 匯入**（共用一份表會讓表格 bug 變成 common-mode，C2 對它完全盲目）。三重前端（Verilator 5.051 / Icarus 14.0 / Yosys 0.64）從第一個 RTL commit 起就跑。
- `2026-07-14-26` test — **G5 = C2：32 組 (Q,W,D) × 86 個 frame × 22,532 個 stage 比對，0 mismatch。** 每個 stage 比對 `bm[4]` / `pm[64]` / `survivor[64]` / **解碼位元**，在 `stage_done` 的脈衝上觸發（不靠數 cycle，這也讓折疊架構將來能零成本沿用同一套 TB）。
- `2026-07-14-27` debug — **C2 抓到的第一個 RTL bug，正是它存在的理由**：`traceback` 被餵了打拍後的 survivor（上一個 stage 的），而 register exchange 的遞迴需要這個 stage 的。症狀極陰險——`bm`/`pm`/`survivor`/`best` **全部完全正確**，只有解碼位元在 frame 頭尾錯掉（256 個位元裡錯 3 個：位置 0、254、255），因為高 SNR 下存活路徑很快收斂。**全零向量完全測不出來**，是全一向量露出來的。只比 bm/pm/survivor 的 C2 會讓這個 bug 完整通過。
- `2026-07-14-28` debug — G6 的 RTL assertion 第一版寫錯：讓影子 PM **跟著 RTL 的決策走**再量 spread。wraparound 一發生，RTL 讓所有狀態都選到錯的分支，影子的 PM 全擠在窄帶裡（Q=4/W=8 是 181–211），**spread 從不變大，assertion 從不響**。改為 `docs/wordlength_bound.md` §5 定義的**決策等價**：影子自己做無界的正確決策，再比對 RTL 的 survivor。
- `2026-07-14-29` test — G6 負向 4/4，全部在 **stage 0** 觸發：Q=4/W=8 spread 181>128、Q=5/W=8 382>128、Q=6/W=8 808>128、Q=6/W=10 776>512。而且這些格點上 **C2 仍然零 mismatch**——RTL 與 golden 錯得一模一樣，這本身就是 C2 有效性最強的佐證。
- `2026-07-14-30` debug — G7（4-state）：cocotb + Icarus 在本機走不通（oss-cad-suite 自帶的 glibc 撐不起 cocotb VPI 要 dlopen 的系統 libpython，GLIBC_2.38；裝系統版 iverilog 需要 root）。改用**檔案驅動**的 SystemVerilog TB，Icarus 這一側完全不碰 Python。10 frames / 2560 bits，C2 零 mismatch 且輸出從未出現 X/Z ⇒ reset 完整。同一支 TB 之後 M5 的 gate-level 模擬也會用到（cocotb 接不上 gate netlist）。
- `2026-07-14-31` add — **Tier B**：C++ harness **沒有 RNG、也沒有量化器**，只重播 L2 匯出的激勵（stimulus.bin + expected.bits + SHA-256 manifest）。規格書 v1 要求的「C++ 的 AWGN 與 L2 位元級一致」做不到（numpy 的 PCG64 + ziggurat 與任何獨立的 C++ RNG 不可能逐位元組相同，除非共用實作——而共用又讓比對變成同義反覆）。這個做法**更強**：只有一份激勵。
- `2026-07-14-32` test — **Tier B 浸泡：12 個點 / 245,760,000 個資訊位元 / 247,200,000 個 trellis stage，解碼位元 XOR 0 mismatch**，SHA-256 12/12 對帳相符。相對 Tier A 擴大 **10,971 倍**。G6 的 assertion 在 2.47 億個 stage 的低 SNR 浸泡中**全程靜默**——而 M3 已證明它在 4 個不安全格點上會於 stage 0 響。Verilator 約 600 kHz（比預估的 2–5 MHz 慢，因為 `--assert` 會把 64-state 的 G6 影子 ACS 也編進去）。
- `2026-07-14-33` audit — 報告必須寫明：**我們不量 RTL 的 BER。** C2 已證明 RTL ≡ golden 逐位元相等，所以兩條 BER 曲線在數學上是同一條；重跑上億位元去「重新量」一條已知的曲線不是驗證，是算術。這是方法學上的強項，不是抄捷徑。

## 2026-07-14（M2）

- `2026-07-14-18` add — GPU golden model（`sweep/viterbi_gpu.py`，torch 整數版，跨 frame batch）與 **C2′ 閘門**（規格書 v1 漏掉的比對點）。24 個測試涵蓋全部 12 個 (Q,W) 格點（含 4 個會 wrap 的不安全格點）、4 個 D、4 個 clip，以及 GPU 編碼器與量化器的逐位元組比對。**零 mismatch。**
- `2026-07-14-19` test — C2′ 的測試證實真的碰到平手：單一組態就有 **85072 次** ACS 平手（Q=3 只有 8 階軟值）。`torch.minimum` 不回傳索引、`torch.argmin` 的平手行為沒有文件保證——這裡改用顯式的鍵 `d*64 + index`，不依賴任何未定義行為。
- `2026-07-14-20` debug — GPU 吞吐的兩次修正。(1) 激勵留在 CPU 時 GPU 空轉（numpy 產 3370 萬個高斯亂數 + 1030 次迭代的編碼迴圈），整體只有 6.7 Mb/s；把編碼器改寫成「移位視窗」一次算完並整段搬上 GPU。(2) survivor 打包成單一 int64 後吞吐從 38.5 **掉到 7.6 Mb/s**——消費級 GeForce 的 **int64 整數運算不是全速率**；改成兩個 int32，最終 **31 Mb/s**（B=32768，1.5 GB），約為 CPU golden 8-worker 總吞吐的 18 倍。
- `2026-07-14-21` test — **設計空間從 (Q, clip, W, D) 塌成 (Q, clip, D)**：W 不是 BER 的軸。這是 G6 的推論（modulo 決策等價 ⇒ 決策與 W 無關 ⇒ 解碼位元與 W 無關），且由 C2′ **直接比對解碼位元**驗證，不是假設。每個 Q 的最小安全 W 由字寬界唯一決定（3→8, 4→10, 5→10, 6→12），PPA 上沒有選擇餘地。
- `2026-07-14-22` test — 全網格 280 點掃完，64 個 (Q,clip,D) 組態全部有 1e-5 交叉點。**M1 的 C1 雜訊地板（±0.076 dB）已被解決**：M2 的 64 格損失全部為正（最小 +0.015 dB），不再有「量化贏過未量化」的物理不可能值。與 M1 交叉驗證：Q=3/clip=2.5σ/D=64 在 M2 量到 **+0.230 dB**，M1（獨立的 CPU 實作、不同亂數串流）量到 **+0.225 dB**。
- `2026-07-14-23` test — **G6 負向展示**：不安全格點的 BER **不降反升**。安全組態 (Q=4,W=10,D=32) 在 4.0→5.5 dB 掉 2.58 個數量級；不安全的 (Q=4,W=8) 在 4→7 dB 反而從 4e-4 **升到 5e-2**，(Q=5,W=8) 與 (Q=6,W=8) 直接釘在 **BER = 0.5（等同擲硬幣）**。機制：高 SNR 時軟值飽和到極值，branch metric 更常打到 λ_max，PM spread 變大 ⇒ wraparound ⇒ 比較反轉。比規格書預期的「高 SNR 神秘 floor」更尖銳。
- `2026-07-14-24` add — 選出 4 組 winner 並記錄理由。刻意**不造綜合成本分數**（真正的硬體成本要等 M5 合成，現在硬掰只會把假設偽裝成結論），改為沿著「所需 Eb/N0」與「已知支配面積的 survivor 記憶體 = 64×3D」挑點：最佳 BER (Q=6,clip=3.0,D=64, 4.152 dB)、最省記憶體 (Q=6,clip=3.0,D=32, 4.194 dB, 6144 bits)、最省 ADC/資料路徑 (Q=4,clip=2.5,D=64, 4.191 dB)、教科書組態 (Q=3,clip=2.0,D=32, 4.359 dB)。

## 2026-07-14（M1）

- `2026-07-14-08` add — 三份凍結文件（`trellis_convention` / `traceback_convention` / `wordlength_bound`）。順序是先驗證再記錄：`verify_trellis.py` 實測 (133,171) 的 d_free = 10、butterfly 結構、以及讓 ACS 只需一個 BM 輸入的兩條互補性。其中一條的成立條件是「每個生成多項式為**奇數**（LSB = 1）」，不是計畫書誤植的「MSB tap = 1」——後者是另一條性質的條件，兩條都成立但理由不同。
- `2026-07-14-09` add — L1 浮點參考（K=7 新寫）與 L2 定點 golden model（`pm_mod` mod-2^W 與 `pm_ref` 無界雙軌）。16 個測試全綠，含四個由強到弱的 oracle：暴力 ML 枚舉、既有模擬器經 mutation testing（90.4%）的 K=3 Viterbi、d_free、定點 vs 浮點。
- `2026-07-14-10` debug — 硬判決 branch metric 恆為 0 或 1、永遠到不了 2：numpy 對兩個 bool 陣列做 `+` 是**邏輯 OR** 而非整數相加，解碼器因此分不出「錯一個位元」與「錯兩個位元」，等於不是 ML。自寫的暴力 ML 測試 10/10 通過（短 frame 蓋不到），是 K=3 oracle 在真實 frame 長度下抓到的（path metric 35 vs 28）。
- `2026-07-14-11` debug — BER 的亂數串流用 `hash(str(cfg))` 當 key，但 Python 對字串的 hash 每個 process 隨機加鹽，同一組態在不同 worker、不同次執行拿到不同 seed，結果不可重現（CLAUDE.md §5.3）。改用 sha256，並加了跨 process 的重現性測試。
- `2026-07-14-12` test — M1 閘門全綠（99 個量測點）。G1 未編碼 BPSK @1e-5 = **9.571 dB**（容差 9.588 ±0.1）；G2b 編碼增益 = **5.434 dB**（區間 [5.0, 5.6]），與 `docs/falsification.md` 事前登記的 5.39 dB 相符，且證實規格書 v1 的 5.0±0.3 確實會紅燈；G3 3-bit 量化損失 = **0.225 dB**（最佳 clip 2.5σ，容差 0.20 ±0.15）。
- `2026-07-14-13` audit — G4 的容差被證明不可能達成：硬判決 union bound（定理，獨立於本專案的量測）給出 **2.355 dB**，本身就落在規格 [1.7, 2.3] 之外。改為兩段式 G4a/G4b，**明確標示為事後修正**（強度弱於 G2 的事前修正）。實測損失 **2.413 dB**。
- `2026-07-14-14` debug — G2a/G4a 的判準本身寫錯兩處：(1) **截斷過的 union bound 不是上界**，`d_max=22` 丟掉的尾巴讓硬判決的界低了 0.3%，改用 d_max=30（≥26 後收斂）；(2) 拿有雜訊的估計值對確定的界做零容忍比較在統計上不成立——界很緊時（實測 G2a 最大 實測/界 = 0.981、G4a = 1.034）正確的解碼器也會有約一半機率因雜訊「超出」。判準改為「95% CI 的下緣不得超出界」。
- `2026-07-14-15` test — D 軸驗證 traceback 慣例：D=24（低於 5K=35）損失 **+0.209 dB**，D=32/48/64 全部落在量測雜訊內。C1 網格在 Q≥4 撞到雜訊地板（**±0.076 dB**，由「量化不可能贏過未量化」的負值直接量得）；Q=3 的損失遠高於地板可信，Q≥4 需 M2 的 GPU 掃描才分辨得出來。如實記錄。
- `2026-07-14-16` add — 凍結 46 個 C2 測試向量（輸入逐位元組 + 期望輸出的 SHA-256）。G6 負向測試 4/4 於 **stage 0** 觸發，實測 PM spread（187 / 401 / 844 / 857）在每一格都低於最壞界卻高於 2^(W−1)——證實界是充分非必要條件，且初始化才是綁住字寬的那一項。
- `2026-07-14-17` debug — 量測的平行度由 14 降到 8：traceback 對 26 MB 的 survivor 陣列做隨機 gather，14 個 worker 讓記憶體頻寬先飽和，總吞吐反而比 8 個低（1578 vs 1686 kb/s），且單 job 慢 1.6 倍（189s vs 117s）。與規格書 §4「不要依賴 Verilator --threads」是同一個道理，只是發生在 numpy 這一側。

## 2026-07-14（M0）

- `2026-07-14-01` audit — 規格書 v1 審查：5 個前提經實測不成立（K=7 浮點參考不存在、RISC-V 的 VCD/SAIF→OpenSTA 功耗流程從未建置、從未用過 OpenLane、Verilator/cocotb/torch 皆未安裝、sky130 cell models 不在 ORFS image 內），6 個技術錯誤（branch metric 取最大/最小自相矛盾、G6 字寬界漏掉初始化、G2 期望值偏低 0.3–0.4 dB、SAIF 不可省略、Icarus 不支援 bind/concurrent SVA、掃描規模漏掉 clip 軸），3 個會改變結果的模糊處。
- `2026-07-14-02` plan — 四項決策定案：執行環境落在 WSL2 原生 FS；traceback 採 sliding-window + best-state 且解碼位元納入 C2 比對集；G2 改為兩段式（G2a union bound 零容忍 + G2b 增益區間 [5.0, 5.6] dB）；能量模型採「規格模型 + 補齊常數 + WSN 標準電路功耗」雙曲線。
- `2026-07-14-03` add — `docs/energy_model.md` 與 `docs/falsification.md` 於任何量測開跑前提交。事前估計 d\* ∈ [13.7 m（模型 A 室內）, 5.4 km（模型 B 自由空間）]，故規格書 §0 的「d\* < 1 m 即證偽」實質不可觸發；追加兩條可證偽的預先登記：Q 3→6 使 d\* 移動 < 5% 則「字寬移動臨界距離」的貢獻宣稱失敗；模型 A 預測 +1.6%～+2.8%、模型 B 預測 −0.5%～−0.9%（**符號相反**）。
- `2026-07-14-04` add — M0 環境建置。Verilator 5.051 + Icarus 14.0 由 oss-cad-suite 安裝（免 root，取代原計畫的「apt + 源碼編譯 Verilator」）；torch 2.11.0+cu128 在 sm_120 上通過整數 ACS 與 numpy 的逐位元組比對，含 123 個平手樣本（證明 tie-break 語意確有被測到）。
- `2026-07-14-05` add — gate-level 功耗流程從零建置（RISC-V 專案零複用）：Yosys 0.64（階層式，不 flatten）→ Icarus + vendored sky130 cell models（commit `ac7fb61f`）→ VCD →`ppa/vcd2saif.py`（FIFO 串流，VCD 不落地）→ OpenSTA `read_saif -scope`。以 8-bit counter 驗收，**activity annotation coverage 100%（117/117 pins，零 unannotated）**。
- `2026-07-14-06` debug — 煙霧測試抓到 `vcd2saif.py` 的 timescale bug：iverilog 把 `$timescale` 寫成跨行，單行 regex 比對失敗後靜默退回預設值 `1 ns`，但實際單位是 `1 ps`，導致 SAIF 的 DURATION 差 1000 倍、OpenSTA 算出的翻轉密度跟著錯 1000 倍。修正後組合邏輯的 switching power 由 1.556e-09 W 變為 1.556e-06 W（正好 1000×），且功耗數字在修正前**看起來完全正常、沒有任何錯誤訊息**。
- `2026-07-14-07` test — M0 驗收閘門 E1/E2/E3 全綠，寫入 `data/gates.csv`。counter 的功耗分佈為 Sequential 88.5% / Combinational 11.5%，與「survivor 記憶體支配 Viterbi 功耗、總功耗對 SNR 可能近乎平坦」的預測方向一致（風險 R1）。

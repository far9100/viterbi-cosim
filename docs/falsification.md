# 證偽條件與事前預測（預先登記）

> **本文件必須在任何能量量測開跑之前 commit**（CLAUDE.md §1.2：「Freeze declarations、tolerance
> definitions 與 final verdicts 必須先於它所管轄的 run 提交，使其 commit 時間戳可被驗證早於該 run」）。
> 一旦 commit，本文件的預測與判準**不得事後修改**。M5 結束時逐條裁決，成立或不成立都如實記錄。

---

## 0. 為什麼要重寫規格書 §0 的證偽條件

規格書 §0 寫的是：

> 在 η_PA ∈ [0.1, 0.5]、2.4 GHz free-space 或 indoor path loss 模型的合理參數範圍內，
> 若 d\* < 1 公尺或不存在，主張即告失敗。

**這個條件實質上不可能觸發。** 兩個理由：

1. **交叉點在數學上必然存在。** ΔE_tx(d) 隨 d^n 單調成長而解碼成本是固定的，故必有一點使兩者相等。
   「或不存在」這半句永遠為假。
2. **d\* 的量級遠大於 1 m。** 用 `docs/energy_model.md` 的常數粗算（見 §2），d\* 落在 13.7 m 到
   5.4 km 之間，跨越所有模型與 η_PA 的組合。距離 1 m 的門檻有一到三個數量級。

也就是說，這條件下的「主張」會**自動存活**，證偽框架失效。這不是失敗，是**門檻設得太低**。

處理方式：**§1 條件 1 原樣保留規格書的條件**（如實跑、如實裁決，不因為預期會過就跳過），
但**追加兩條真正咬得住的條件**（條件 2、3），指向本專案真正的貢獻——「字寬如何移動 d\*」。

---

## 1. 三條預先登記的證偽條件

### 條件 1（沿用規格書 §0，原文保留）

> 在 η_PA ∈ [0.1, 0.5]、2.4 GHz free-space 或 indoor path loss 模型的合理參數範圍內，
> 若 d\* < 1 公尺或不存在，主張即告失敗。

**事前預測：不會觸發。** 見 §2 的估計表。若竟然觸發，代表能量模型或 PPA 量測有嚴重錯誤，
須先追查錯誤而非宣稱發現。

### 條件 2（新增，針對頭條貢獻）

規格書的交付圖表 4 是「d\* vs Q——字寬如何移動臨界距離」，DoD §6 的結論句型是
「Q 從 3 bits 增至 5 bits 使 d\* 從 ___ 移到 ___」。這個句型**預設了 d\* 會明顯移動**。

**若 Q 從 3 增至 6 使 d\* 的變動量在模型 A 與模型 B 下皆小於 5%**，則「字寬移動臨界距離」
這個貢獻宣稱**失敗**。報告須如實改寫為：

> 效應存在但微弱：量化增益與解碼器能量對 d\* 的作用方向相反（見 §3），近乎相消。

### 條件 3（新增，針對本文件自己的事前估計）

**若實測的 d\* 對 Q 的變動量在模型 A 下超過 30%，或變動的「符號」與 §3 的預測相反**
（模型 A 應為正、模型 B 應為負），則本文件的事前機制推理被推翻。這是**有價值的正面結果**，
須追查機制，不得靜默調整模型去迎合數據。

---

## 2. d\* 的事前估計

由 `docs/energy_model.md` 的模型：

```
d*^n = [ E_dec(Q) + ΔE_circ ] · η_PA  /  [ ΔEbN0(Q) · N0 · L₁ ]

L₁      = 1.01174e4                       # 1 m 處的路徑損耗
N0      = 1.5940e-20 J                    # k·T₀·F，F = 6 dB
ΔE_circ = 0（模型 A） 或 P_c/R_s = 60 nJ（模型 B）
ΔEbN0(Q) = (Eb/N0)_未編碼 − (Eb/N0)_編碼(Q)      # 線性值，不是 dB
```

事前輸入（**這些是預測，不是量測——M1/M5 會把它們換成實測值**）：

| 量 | 事前值 | 依據 |
|---|---|---|
| (Eb/N0)_未編碼 @1e-5 | 9.588 dB = 9.096 | 閉式解；既有通訊模擬器已量到 9.5842 dB [9.5775, 9.5906] |
| (Eb/N0)_編碼 @1e-5（未量化、D=64） | 4.2 ± 0.15 dB = 2.630 | (133,171) union bound，d_free=10，c_d = 36/211/1404/11633… |
| ⇒ 編碼增益 | **5.39 dB** | 落在 G2b 的 [5.0, 5.6] 區間內 |
| 3-bit 量化損失 | +0.20 dB | Heller & Jacobs |
| 6-bit 量化損失 | +0.02 dB | |
| E_dec | ~100 pJ/info bit | 量級估計；Sky130、64-state、flop-based survivor mem |

**估計出的 d\***（E_dec = 100 pJ，F = 6 dB）：

| 模型 | η_PA | 自由空間 (n=2) | 室內 (n=3.5) |
|---|---|---|---|
| A（僅 PA + 解碼器） | 0.1 | 98 m | **13.7 m** |
| A | 0.5 | 219 m | 21.7 m |
| B（+ 電路功耗 + 2× airtime） | 0.1 | 2.40 km | **85 m** |
| B | 0.5 | 5.37 km | 135 m |

全部遠大於 1 m ⇒ **條件 1 預測不會觸發**。

注意模型 B 的 d\* 比模型 A **大**，不是小。加入電路功耗後，R=1/2 的編碼鏈路要付兩倍空中時間、
兩倍電路能量（60 nJ，比 E_dec 的 0.1 nJ 大 600 倍），編碼變得**更不划算**，交叉點因此往外推。

---

## 3. Q → d\* 的事前預測（本專案的頭條）

### 3.1 兩個方向相反的機制

Q 增加時，有兩件事同時發生，**方向相反**：

| 機制 | 方向 | 大小 |
|---|---|---|
| 量化損失變小 ⇒ 編碼增益變大 ⇒ ΔEbN0 變大 ⇒ 省更多發射能量 | **d\* 變小** | ΔEbN0(3)/ΔEbN0(6) = 6.342/6.454 = 0.9827 |
| PM 字寬 W 變大（8→12）⇒ ACS 資料路徑變寬 ⇒ E_dec 變大 | **d\* 變大** | 取決於 ACS 佔 E_dec 的比例 α |

令 **α = E_dec 中隨 W 縮放的比例**（ACS + PM 暫存器 + BMU），(1−α) 為 survivor 記憶體
（與 Q、W 皆無關）。W 從 8 到 12 是 1.5×，故：

```
E_dec(Q=6) / E_dec(Q=3) = (1 − α) + 1.5α = 1 + 0.5α
d*(Q=6) / d*(Q=3) = { (1 + 0.5α) × 0.9827 }^(1/n)
```

### 3.2 預測（模型 A）

| α（ACS 佔 E_dec 比例） | 自由空間 (n=2) | 室內 (n=3.5) |
|---|---|---|
| 0.15（**本專案的預測**：survivor 記憶體支配） | **+2.8%** | **+1.6%** |
| 0.50 | +10.8% | +6.1% |
| 0.85（ACS 支配） | +18.3% | +10.1% |

**最佳事前估計：α ≈ 0.15，故 d\* 隨 Q 增加約 +2% 至 +3%。**

α ≈ 0.15 的依據：D=32 時 survivor 記憶體是 64 states × 3D = **6144 個 flop**（Sky130 DFF ≈ 20 µm²
⇒ ~123,000 µm²），對上 ACS 的 640 個 PM flop + 32 個 butterfly（≈ 23,000 µm²）——面積上
**約 85% 是 traceback 記憶體**。能量的分佈未必與面積相同（clock gating 會大幅改變它），
所以 α 是本專案 M5 要**量出來**的東西，此處只給預測。

### 3.3 預測（模型 B）

模型 B 的 ΔE_circ = 60 nJ 完全淹沒 E_dec 的 0.1 nJ 級變化：

```
[E_dec(6) + 60000 pJ] / [E_dec(3) + 60000 pJ] ≈ 1.00012
d*(Q=6)/d*(Q=3) = { 1.00012 × 0.9827 }^(1/n)
```

| | 自由空間 (n=2) | 室內 (n=3.5) |
|---|---|---|
| 模型 B | **−0.87%** | **−0.50%** |

**模型 B 下 d\* 隨 Q 略微「減少」——與模型 A 的符號相反。** 因為 E_dec 的效應被電路能量淹沒，
只剩量化增益的效應。

### 3.4 這是一個「符號會翻轉」的預測

這是本文件最咬得住的一條：**同一組 RTL、同一組功耗量測，在兩個能量模型下 d\* 對 Q 的斜率符號相反。**
若實測的符號與此不符（模型 A 為負、或模型 B 為正），代表機制理解錯誤，須追查（條件 3）。

而且，由 §3.2 的表可以看出：**測到的 d\* 對 Q 的斜率，反過來就是 α 的量測**——也就是
「解碼器的能量到底花在 ACS 還是花在 traceback 記憶體」。這是這條驗證鏈路真正能回答、
而純軟體模擬回答不了的問題。

---

## 4. 與已核准計畫的差異（必須說明）

本專案的執行計畫（`fec-viterbi-cosim-spec-md-effervescent-hamster.md` §2.1）曾預測
「模型 A 下 d\* 移動約 7–12%」。**那個數字在此被修正為 +1.6% 至 +2.8%。**

原因：該估計用了 `E_dec(6)/E_dec(3) ≈ 1.2`（隱含 α ≈ 0.4），但同一份計畫的 §5/M5 又預測
survivor 記憶體佔 85%（即 α ≈ 0.15）。這兩者互相矛盾。本文件採用後者，因為它有具體的
flop 計數依據。7–12% 對應的是 α ≈ 0.5 的情形，已列在 §3.2 的表中作為對照。

此修正發生在**任何量測開跑之前**，不是事後調整。

---

---

# ▼▼▼ 以下為 M5 完成後追加的裁決。§0–§4 為預先登記內容，一字未改。▼▼▼

## 5. 裁決（2026-07-14，M5 完成）

資料來源：`data/gates.csv`、`data/results_m5_power.csv`、`data/results_m5_dstar.csv`。
可由 `python3 scripts/m5_gate.py` 完整重生。

### 5.1 三條證偽條件

| 條件 | 判準 | 實測 | 裁決 |
|---|---|---|---|
| **F1** | d\* < 1 m 或不存在 ⇒ 主張失敗 | 所有模型/環境/η 下的**最小** d\* = **17.8 m** | **不觸發 → d\* 主張存活** |
| **F2** | 兩個模型下 \|Δd\*\| 皆 < 5% ⇒ 貢獻宣稱失敗 | 模型 A：**+11.29%**（自由空間）/ **+6.31%**（室內） | **不觸發 → 貢獻宣稱存活** |
| **F3** | 模型 A 為負、或模型 B 為正、或 \|A\| > 30% ⇒ 事前推理被推翻 | A **正**（+11.29 / +6.31%）、B **負**（−0.75 / −0.43%），且 A < 30% | **不觸發** |

**§3.4 的「符號會翻轉」預測確認成立。** 這是本文件自稱「最咬得住的一條」：

| | 事前登記 | 實測 |
|---|---|---|
| 模型 A（自由空間 / 室內） | +2.8% / +1.6% | **+11.29% / +6.31%** |
| 模型 B（自由空間 / 室內） | **−0.87% / −0.50%** | **−0.75% / −0.43%** |

**模型 B 的量級幾乎完全命中**（−0.87 vs −0.75；−0.50 vs −0.43）。符號在兩個模型下相反，
與 §3.1 的機制推導一致。

### 5.2 α 的裁決 —— **我的點估計錯了 3.4 倍，如實記錄**

§3.1 定義 `E_dec(Q=6)/E_dec(Q=3) = 1 + 0.5α`。實測（Q=3/D=32 vs Q=6/D=32，
**同一個 D ⇒ 完全相同的 traceback**）：

```
E_dec(Q=3, W=8,  D=32) = 24.090 mW / 100 MHz = 240.90 pJ/bit
E_dec(Q=6, W=12, D=32) = 30.321 mW / 100 MHz = 303.21 pJ/bit
比值 = 1.2586    =>    α = 0.517
```

**事前登記 α ≈ 0.15。實測 α = 0.517。差了 3.4 倍。**

一個漂亮的旁證：**同一個 D 下，traceback 的功耗實測是 13.055 mW（Q=3）vs 13.044 mW（Q=6）
—— 相差 0.08%。** register exchange 的活動量確實與 Q、與資料完全無關，如 §3.1 所假設。
所以 α 的誤差**不是**來自 traceback 項，而是來自我對「隨 W 縮放的那一塊」估得太小。

**兩個錯誤（都是我的）：**

1. **用 flop 數去估功耗佔比。** §3.2 的依據是「面積上約 85% 是 traceback 記憶體」。
   面積確實如此（實測 traceback 佔 67.7–84.1% 的 flop），但**功耗不是**：
   traceback 只佔 **43.0–54.2%** 的功耗。原因是 ACS 與 min-PM 有大量**組合邏輯**
   （加法器、比較器、argmin 樹），它們燒 switching power 但不是 flop。
   §3.2 自己寫了「能量的分佈未必與面積相同」，但我還是用了面積的數字當點估計。

2. **完全漏掉 min-PM 的 argmin 樹。** α 的定義是「ACS + PM 暫存器 + BMU」——
   **我根本沒把 min-PM 列進去**。而它同樣隨 W 縮放（它比較的是 W-bit 的路徑度量）：

   ```
   min-PM 功耗：2.482 mW (W=8)  ->  4.081 mW (W=12)   +64.4%
   （W 只增加 50%，功耗卻增加 64% —— 超線性）
   ```

   它佔 **10.3–13.5% 的功耗**、**12–20% 的面積**——**比整個 PM register file 還大**。
   這是 best-state traceback（相對於 fixed-state）的直接代價，也是本專案的意外發現之一。

**誠實的補充：§3.2 的表本來就列了 α = 0.50 那一列（+10.8% / +6.1%），
而實測（α = 0.517）正好落在那裡（+11.29% / +6.31%）。模型是對的，錯的是我餵進去的 α。**
換句話說，§3.1 的公式與 §3.4 的符號推理都通過了檢驗；被推翻的只有 §3.2 的**點估計**。

### 5.3 ADC 能量（`docs/energy_model.md` §6.2 承諾的敏感度線）

`E_ADC / info bit = 2 · FoM · 2^Q`（R=1/2 ⇒ 每 info bit 2 次轉換）。**敏感度線，非量測值。**

| Walden FoM | E_ADC @ Q=6 | 佔 E_dec+ADC | 模型 A / 室內：Δd\*（Q3→Q6） |
|---|---|---|---|
| 10 fJ（state-of-the-art） | 1.28 pJ | 0.4% | +6.31% → +6.42% |
| 100 fJ（實務） | 12.80 pJ | 4.0% | +6.31% → **+7.36%** |
| 500 fJ（較舊 / 高速 flash） | 64.00 pJ | 17.4% | +6.31% → **+11.27%** |

ADC 能量與 E_dec **同向**（都隨 Q 增加）⇒ 只會**加大** Δd\*，**不會**翻轉符號。
**所以把 ADC 算進來只會讓 F2 的結論更穩，不會動搖它。** 模型 B 幾乎不受影響
（−0.43% → −0.40%），因為 60 nJ 的電路能量把一切淹沒。

### 5.4 本次裁決**沒有**涵蓋的東西（不得默示為已驗證）

* **規格書 §7 的「功耗 vs SNR 依賴曲線」交付物被實測推翻**（總功耗只變動 1.0%、非單調、
  方向與規格書前提相反）。這**不是** F1/F2/F3 的任何一條，是一個獨立的負面結果。
  機制與反事實見 `scripts/diag_mechanism.py` 與規格書 §7 的修訂。
* d\* 的絕對值仍是**上界**：survivor 記憶體用 flop 陣列而非 SRAM macro
  （`docs/energy_model.md` §6.1）。Q 之間的**相對**比較不受影響（同一個 D 下 traceback 完全相同）。
* Fmax 是 post-placement / pre-route，**樂觀**（`data/results_m5_fmax.csv`）。

---

# ▼▼▼ 以下為凍結後追加的勘誤。以上為凍結原文，一字未改。▼▼▼

本節由 `docs/errata.md` 索引，並由 `scripts/check_paper_numbers.py` §6 雙向對帳。
凍結本體（第一個 ▼▼▼ 之前）另有逐位元組對照凍結 tag 的檢查。

## 勘誤 E-04（2026-07-31）

**原文（§5.4）**：「d\* 的絕對值仍是**上界**：survivor 記憶體用 flop 陣列而非 SRAM macro
（`docs/energy_model.md` §6.1）。Q 之間的**相對**比較不受影響（同一個 D 下 traceback 完全相同）。」

**更正**：第一句成立，**第二句的方向反了**。
traceback 能量在 Q3 與 Q6 之間確實是共模項，但正因為它是共模項，
用 SRAM macro 把它縮小會讓兩者的 E_dec **差值佔比變大**，於是 Δd\* **變大**。
也就是說已報的 Δd\*（模型 A/free_space **+11.29%**）是一個**下界**，
而不是「不受影響」。這使 F2 的裁決比原本以為的更穩健，不是更弱。

**證據**：`data/results_m5_tb_sensitivity.csv` —— `delta_dstar_pct` 隨 `tb_factor` 下降而單調上升。

**發現於**：CHANGELOG `2026-07-29-06`。
